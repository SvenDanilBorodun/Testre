# Known issues, open work, and rig-validation status

Living status for EduBotics — the half of the old `CLAUDE.md` that changes weekly.
Split out 2026-08-02 so it stops riding every session's context.

**Read this when** the task touches: edu6 / Edu:1 hardware bring-up, Orange-Pi
parity, rig gates (R1-R10 for edu6, E1-E8 for Edu:1), dead code, stale comments,
branch pruning, or deferred operator runs.
It is *status*, not invariant — `CLAUDE.md` holds the rules, this holds what is
currently broken, unproven, or deliberately unfixed.

Curated 2026-07-03; Orange-Pi entries appended 2026-08-02; the rest not re-curated.

`CLAUDE.md`'s **"cite symbols, never line numbers"** rule is not yet uniformly applied in THIS file: the Orange-Pi networking bullet and the RUNBOOK pair below still carry `:NN` citations, deliberately, because they are recorded as out-of-scope residue whose whole point is *which* lines were and were not fixed. Every OTHER citation here should be a symbol, and new ones must be.

---

- **The re-lock / warned-glide round (2026-09-13) — what is NOT verified, and what it deliberately does not do.** Software-complete and green (server `test_manual_relock_home_glide.py`, React `HomeGlidePrompt.test.jsx` + the RecordPanel/JogPanel/TableTouchStep/RunControls additions, each mutation-checked); nothing below has run on a rig.
  - **RIG GATE H1 — the OMX hold-before-torque has never run on hardware.** The mechanism is the collision monitor's relax-in-place (a single-point trajectory at the measured pose), but issued while torque is OFF, and the claim that the JointTrajectoryController adopts it within `_TORQUE_ON_HOLD_SETTLE_S` (0.2 s) with the arm limp is an inference from the controller's documented sampling, not a measurement. **Check on an OMX**: Startseite activation (JTC now holds HOME) → Roboter Studio → „Tisch vermessen" → tap four points → „Berechnen & speichern": the arm must stay at the last tap, then glide after the countdown. Same for „Arm freischalten" → move → „Arm festsetzen". If it still snaps, first suspect the settle (raise it) and the `set_dxl_torque` path in the base image's `dynamixel_hardware_interface`, whose source is not in this repo.
  - **RIG GATE H2 — the manual home glide on each arm.** Floor-planned like the `home` block, but a hand-guided start is a WIDER start population than a program ever produces (the student can leave the arm anywhere). Watch for the planner's German refusal from genuinely awkward poses on edu6/edu1, and for the OMX direct leg's elbow (only the TCP was measured).
  - **DELIBERATE: the countdown auto-starts** (owner decision, 2026-09-13), 5 s, with „Hier stehen lassen" as the opt-out. A confirm-click-only variant was offered and declined.
  - **RIG GATE H3 — confirm the replay fix on an OMX.** Replay used to publish per-point velocities, and the JointTrajectoryController drops any message whose last point is still moving (13 of 14 chunks of a 15 s take — „the robot did not move at all"). Replay is now position-only. On a rig: record ~15 s, „Vorschau abspielen" — the whole motion must play, and `docker logs` of the open_manipulator container must show no „Velocity of last trajectory point" / „not strictly increasing" lines. `ros2 param get /arm_controller allow_nonzero_velocity_at_trajectory_end` should read false.
  - **DONE (owner-approved) — replay time-stretch.** Recorded pairs are now joined linearly (`build_linear_segment`) with the 0.6 × limit floor on the commanded velocity; a 2.8 rad/s take replays in its recorded time (was +76 %). RIG: watch brisk replays for servo lag on the Feetech arms (`WRITE_ACCELERATION`, below) — a faithful timeline now demands more acceleration than the stretched one did.
  - **DONE (owner-approved) — collision recovery re-energises with a hold, awaited.** RIG GATE H4: the `RebootDxl` request field name is still guessed (`id` / `dxl_id` / `ids`), and whether `reboot_dxl` re-applies the xacro's RAM Profile Velocity/Acceleration is unknown without the base image's hardware interface — after a firmware overload trip, confirm the rebooted joint glides smoothly rather than at an unprofiled speed.
  - **RIG CHECK FIRST — gripper sign in the leader sync and the collision resync (OMX audit MED-2, PLAUSIBLE).** `activation_agent._sync_to_leader` and `resume_teleop`/`force_resume_teleop` target the RAW `/leader/joint_states` `gripper_joint_1`, while the broadcaster negates that joint (`reverse_joints`), which would glide the follower gripper to ≈ −0.7 (closed, stalling) and then step ≈ 1.4 rad when teleop starts. Compare `/leader/joint_states` vs `/leader/joint_trajectory` for `gripper_joint_1` on a rig before touching it.
  - **DONE (owner-approved) — the Blockly `home` block uses the floor-checked planner on the OMX too.** Measured cost (final verifier C, real IKSolver, floor z = 0): strict-vertical grasp/hover/drop poses 0 refusals of 77,178 and 2.1 % lifts; RANDOM poses with the TCP 0–60 mm up (what a replay or hand-guiding can leave) 11.1 % refuse and 22.7 % lift — a `spiele Bewegung ab` → `home` program can now stop with the German hand-lift refusal, in the simulator too. The seam refusal (`refuse_wrapped_joint_jumps`) also judges OMX joints 1/4/5 (their solver limits span ±π); a false refusal needs the recorder starved for >1 s during a >π wrist turn. OPEN, same class: `go_to_observation_pose` (the „Solange sichtbar" retreat, default target HOME) still uses `plan_home_route` and the OMX's unjudged direct leg.
  - **OPEN, rig — Feetech `WRITE_ACCELERATION = 50` (7.67 rad/s²) likely lags brisk replays** (modelled goal error up to 0.20–0.30 rad; 0.08–0.15 at 254). Needs R5.
  - **OPEN, LOW — a ~1 s chunk boundary is a small hitch on both rails** (Feetech applies a chunk's first point at once: 0.01–0.09 rad; the OMX restarts from the measured state). Repeating the previous chunk's last point at t = 0 would fix both but touches every workflow motion — rig measurement first.
  - **OPEN, LOW — nothing verifies arrival for jog / Blockly moves / manual replay** (they report success once published); the collision home glide is the only verified-and-resent path. The home glide checks arrival (warn-only).
  - **OPEN, LOW — the pre-torque hold only runs when `_follower_torque_on is not True`**; a timed-out torque-off that actually landed, or a servo brown-out, leaves the flag True and skips it. Holding on every OMX torque-on would add ~0.27 s to every jog nudge.
  - **Final end-to-end simulation (verifier B, real code, all four arms, ROS stubbed): 0 controller rejections, 0 silent no-motion, first-point offset ≤ 0.8 mrad, replay timing 1.000–1.0025 of the recording, published velocity exactly at the 0.6 × limit ceiling.** Residuals it measured, each needing a rig:
    - **Chunk-boundary bumps at the RECEIVER** (pre-existing mechanism): the Feetech driver applies a chunk's first point at once (a one-waypoint goal jump every ~1.2 s, modelled servo peak 3.47 rad/s against the 3.27 ceiling on a brisk edu6 take); the OMX controller restarts each chunk from the measured state (up to 1.26× the commanded speed at 10 ms lag). Repeating the previous chunk's last point at t = 0 would address both but changes every workflow motion.
    - **OMX floor-checked home refusals from hand-left poses**: 35 of 150 arbitrary near-table poses refused (the branch-swapping IK lift refused 12, by swapping branches). Tap and grasp/drop starts are unaffected; the refusal names the hand-lift remedy.
    - **The point model assumes OMX base z = 0 is the table surface**; a riser under the base or a tilted table is not modelled.
    - **Brisk Feetech replays may exceed what `WRITE_ACCELERATION = 50` lets a servo follow** (trapezoid model lag up to 1.35 rad edu6 / 0.56 rad edu1). The stream is correct.
    - **Re-lock residual with starved `/joint_states`** (the touch-off solve on the default group): the stale hold leaves an 82 mrad snap at a 0.3 rad/s sag, against 22 mrad with fresh data.
    - **A fast gripper squeeze stretches the whole OMX replay vector** (the floor is judged on every joint incl. the gripper): +1.9 % on a 15 s take.
    - **A real program start right after boot publishes a hold** (`_follower_torque_on` unknown), which cuts short a Startseite activation glide started in the last ~3 s.
    - **The seam checks assume XL330 joints 4/5 wrap at ±π when hand-turned** (they share the ±π solver limit with edu6 joint4/6); unverified.
  - **NOT FIXED — replay on the OMX restarts each ~1 s chunk from the MEASURED state.** The controller interpolates a newly received chunk from `state_current_` (closed loop), which trails the command by the servo profile, so fast recordings may hitch at chunk boundaries. PLAUSIBLE, unmeasured; the only clean fix (interpolate from the desired state) would re-open exactly the stale-reference snap fixed here, so it needs a rig measurement before anyone touches the controller YAML.
  - **NOT FIXED — fast hand motion replays time-stretched.** `resegment_trajectory` applies the quintic 15/8 peak factor to every 40 ms recorded pair, so any joint moving faster than ~1.54 rad/s is slowed (measured on the real function: 1.28× total duration at a 2.63 rad/s peak, 1.75× at 3.95 rad/s; ≤ 1.01× at 1.64 rad/s). The PATH is preserved. Relaxing it is a change to the replay velocity floor (Rule §2 — ask first).
  - **NOT FIXED — a record stop whose re-torque fails discards the take** (the server returns no `points_json` on that path). The arm-safety half is correct; returning the buffer anyway would be a UX improvement.

- **The smooth-twin round (2026-09-11) — what is NOT verified, and what it deliberately does not do.** The sim plays each chunk in real time (`SimArm` `_TrajectoryPlayer`) and every `UrdfTwin` draws an interpolated pose (`utils/jointStateInterpolator.js`); both halves are unit-tested, three independent verifiers re-derived the numbers, and the end-to-end effect was measured against the real server code with a MODELLED rosbridge queue (see the changelog) — not in a browser on a rig.
  - **NOT MEASURED: render cost on a classroom PC.** A moving arm now redraws at the display's frame rate (it redrew only when a message landed), and an arm at rest reporting ±1 encoder tick of flicker redraws a few times a second. The meshes are 68k–129k triangles and the sim stage adds a 1024² PCF shadow pass, which an integrated GPU should carry, but WebView2 on the cheapest school laptops is unmeasured. If a rig stutters, the levers are the shadow map and `setPixelRatio`, not the interpolation.
  - **DELIBERATE: every twin now lags reality by `INTERP_DELAY_MS` (100 ms) plus transport.** That is what buys the blend margin. It is a monitor view, not a control surface; the Startseite activation button and the collision modal are unaffected (both are ROS-side decisions, not pose readings).
  - **DELIBERATE: the end-of-run reset JUMPS the virtual arm to HOME** (and the cubes back to their placement) rather than gliding — the existing „run over = back at the start" design (`_on_sim_workflow_finished`). It is published AS a jump (`_publish_sim_teleport`), so the twin draws it as one. A glide would have to come from the server (so the twin and the next run agree on where the arm is) and be cancelled by a run start; owner call.
  - **DELIBERATE: `/sim/objects` is aligned with the drawn arm by a FIXED 100 ms delay, not by stamp.** The residual is the scene message's extra latency over the fastest joint delivery (a few ms, plus up to the 20 ms throttle): a capture in a replay that closes the jaws WHILE MOVING can attach the cube a few millimetres off the jaws. Stationary closes (every composite grasp) are unaffected. The stamp-exact fix is a `stamp` field in the scene JSON consumed on the twin's own render clock.
  - **Known, tiny: a player frame that is more than `_SIM_HEARTBEAT_QUIET_S` late** (the playback thread starved for ~0.5 s) can be stamped behind a heartbeat by more than `_SIM_STAMP_REORDER_S`, and the twin then drops that ONE waypoint as out-of-order. The nudge covers the ms-scale case (a frame that waited on `_sim_pub_lock`); a half-second starvation is not worth a second mechanism.
  - **Stop in the simulator ends at the reset, not after the in-flight chunk.** On the rig the JTC finishes the in-flight chunk (≤ 1 s) after a Stop; in the sim the reset's `cancel()` cuts it and HOME follows. Both are the documented behaviour of their own path; they are not the same.
  - **`test_sim_arm_realtime.py` runs on the real clock** (the player waits on a `Condition`), with tolerances of tens of ms that separate "played over the span" from "dumped in a burst" by three orders of magnitude. A pathologically loaded CI runner is the one way it could flake. Its end-to-end test deliberately requires EVERY waypoint (no `len - 1` slack): that slack is exactly what let a dropped chunk-final frame through.

- **The activation gate (2026-09-07) — what is NOT verified, and what it deliberately does not do.** The code is complete and green across all four layers (`entrypoint_omx.sh`, `omx_l_leader_ai.launch.py`, `edu6_arm_node.py`, `activation_agent.py`); what follows is what a rig still has to answer and what was consciously left out.
  - **RIG GATE A1 — the deferred controller spawn has never run on hardware.** `activation_agent._start_teleop` runs `ros2 run controller_manager spawner joint_trajectory_command_broadcaster -c /leader/controller_manager` — the same command the leader launch already runs successfully, but LATER and with an explicit `-c` because the launch's `PushRosNamespace('leader')` is not in scope from outside. The controller's own parameters (`joints`, `reverse_joints`, `offsets`) come from the `/**:`-wildcard YAML loaded into `control_node`, i.e. from the controller_manager's parameter overrides, which is why this should be identical — but it is an inference, not a measurement. **Failure mode is benign and reported**: activation stops at the `teleop` step with a German `[FEHLER]`, the follower is already homed and torqued, and the student can retry (verified end to end against real rclpy in the built image — `spawner rc=1 … Could not contact service /leader/controller_manager/list_controllers`, terminal in 41 s of the 60 s budget, retry accepted).

  **MEASURED INSIDE THE IMAGE 2026-09-07, which narrows this gate to one question.** `ros2 run controller_manager spawner --help` accepts both `-c` and `--controller-manager-timeout`, so the CLI form is valid. `grep robot_description` over the installed `/opt/ros/jazzy/…/controller_manager/spawner.py` returns **zero hits** — the boot spawner's `parameters=[{'robot_description': urdf_file}]` is already INERT for the spawner, so the deferred call is missing nothing on that axis. And without `-u` the spawner `return 0`s straight after activation (spawner.py, the `--unload-on-kill` branch), so `subprocess.run(timeout=60)` cannot hang. **The only residual is whether the controller's own `joints`/`reverse_joints`/`offsets` reach it through the controller_manager's parameter overrides** — they are declared under the `/**:` wildcard in `omx_l_leader_ai/hardware_controller_manager.yaml`, which is loaded into `control_node`, i.e. into the manager that creates the controller, which is why this should hold. If it does not, `--param-file` on that command is the fix, **not** putting the controller back in the boot spawner.
  - **RIG GATE A2 — the whole sequence, end to end, on each of the four profiles.** Specifically: that `/joint_states` is flowing by the time a student can press the button (the agent waits 10 s and then fails in German); that the OMX home glide from a POWER-UP pose is as tame as the one from the old boot path (it is the same quintic, from a pose that is now whatever the arm powered up in rather than whatever it powered up in ten seconds ago — the same set); and that a Feetech `/edubotics/home_arm` round trip lands inside the agent's 20 s service wait.
  - **The Feetech „active" claim is a TIMED claim, not an arrival signal.** `_home_feetech` calls the driver, then sleeps `MOVE_DURATION_S + SETTLE_S` and reports `active`. A REFUSED glide does come back as `success=false` with the driver's own German reason (floor refusal, edge-parked joint, 12 V), so only the „it arrived" half is soft; the driver's own `_boot_home_verify` still logs the truth to the Protokoll. Making it a real arrival signal means the driver reporting completion, which is a bigger contract than this round needed.
  - **DELIBERATELY NOT DONE: recording, inference and Roboter-Studio runs are NOT blocked on an un-activated rig.** The server is unchanged. A student who ignores the Startseite button and goes straight to Aufnahme records a follower that never moves. The card is the only thing in the way. Trigger to revisit: the first classroom that reports a dataset of a still arm.
  - **`/edubotics/home_arm` cannot actually be entered twice, and the earlier note here saying it could was wrong about the mechanism.** `edu6_arm_node.main()` uses `rclpy.spin(node)` (single-threaded) and `create_service(Trigger, HOME_SERVICE, self._home_cb)` passes no callback group, so `_home_cb` runs on the node-default MutuallyExclusive group: two callers serialise. The agent additionally refuses a CONCURRENT `/edubotics/activate`. What IS true is that the SECOND caller then gets a success for a glide the first one started.
  - **A re-home while the arm is being hand-guided is not fenced, and the obvious fence would be worse.** `run_home` re-torques a limp arm by design (hand-guide's „Beenden" is not the only way a student gets there with torque off), so „Grundstellung erneut anfahren" pressed during a live hand-guide or a touch-off would energise and glide an arm somebody's hands are on. The agent lives in the other container and claims nothing from the manual-mode arbiter, so it cannot see that state. `selectCalibrationHasUnsolvedCaptures` was evaluated as the client-side fence and REJECTED: `framesCaptured` LATCHES and its own docstring says the caller must also establish that the wizard is on screen — never true on the Startseite — so wiring it in would kill the button permanently after one abandoned wizard, a worse failure than the one it prevents (`ActivationCard::blockReason` carries the reasoning, and a test pins it). Standing in for it today: the wizard's unmount cancels the server-side capture buffer, the manual arbiter's 30 s idle watchdog re-torques an abandoned session, the re-home offer is HIDDEN on leader rigs, and the button is refused while a task or a workflow runs. The real fix is the agent participating in the arbiter, which needs a cross-container claim this round did not build.
  - **The collision e-stop is ARMED on an un-activated both-arms rig, and that is a new reachable state.** `leader_appears_active()` keys solely on `/leader/joint_states` freshness, and the leader's `joint_state_broadcaster` still spawns at boot — only the TRAJECTORY broadcaster is held back. So on an idle, un-activated `omx_full` rig a hand-press on the still follower can satisfy the detector (effort ≥ threshold at |v| ≤ 0.05 rad/s, 150 ms debounce, no workflow/manual/inference gate active), trip `/collision_flag`, and put the student into the two-step COLLISION recovery for a robot that was doing nothing. It is recoverable (HOME_FOLLOWER and the RESUME_TELEOP resync publish directly, not through the broadcaster) and it was already reachable before the gate once the boot home had finished — what changed is that the arm now sits at an arbitrary power-up pose, which is likelier to invite a nudge. **Narrowing the predicate is an ask-first change to collision gating (Rule §2), so it was NOT made**; the stale docstring in `collision_monitor.leader_appears_active` was corrected instead.
  - **A mode toggle DE-ACTIVATES the robot, by construction.** `restart_open_manipulator` recreates the arm container, so the gate is back at `idle`: the arm holds its pose and, on a leader rig, teleop is not running. `LeaderToggle` says so in its overlay and in a toast, because nothing on the Roboter-Studio page can activate — that has to be a press on the Startseite.
  - **A joint feed that dies mid-activation is now caught, but only because the cache expires.** `_follower_pos`/`_leader_pos` are age-checked (`JOINT_MAX_AGE_S`, 2 s); before that they were never invalidated, so a feed delivering one sample at boot and then dying still built the quintic from that stale pose and the only symptom was the soft „did not quite arrive" warning. The old entrypoint blocks had the same shape, so this is a fix rather than a regression — but note the arrival check is still soft by design, so an arm that is mechanically blocked ends `active` with a `[WARNUNG]` (the card renders that amber, not green).
  - **An OLD image (no agent) leaves the card on „unbekannt" with the button live**, and the call then fails with the client's own German „konnte nicht erreicht werden". That is the intended shape: `status === null` must never render as „nicht aktiviert".
  - **A CRASHED agent is bounded, not unbounded.** The entrypoint supervises it (5 runs in total — the first plus four restarts, 2 s apart — gated on a teardown sentinel so a clean SIGTERM exit is not mistaken for a crash) and gives up with a German `[FEHLER]` naming „Umgebung neu starten". It is the only helper in that entrypoint that is supervised, because its failure is the only one that is both SILENT (the healthcheck proves topics exist, not that the rig can be activated) and TOTAL (no motion at all, for the whole session). What is still true: if it gives up, the Protokoll is the only place that says so — the card just stays on „unbekannt".
- **Absorbed-upstream C++ sits outside the parity gate's view.** `image-source-parity` verifies overlays by sha256 only; unmodified absorbed sources (e.g. `om_joint_trajectory_command_broadcaster`, whose stock `/collision_flag` skip-publish the e-stop relies on) reach the image via the ROBOTIS base clone. If such a file is ever EDITED in-repo it must join the overlay chain or the drift is invisible.
- **Rig-validation-pending from v2.11** (physical, not code): jog cadence/torque latency, `drive_to` jaw convention, replay floor-refusal margin at the table, leader-gate behaviour on a real collision. Related: `EDUBOTICS_GRASP_ROLL_DEG`'s 90° jaw convention has no runtime guard — rig-level gate only.
- **Rig-validation-pending from the robot-type system (2026-07-10)**: both profiles boot end-to-end; `omx_follower` shows only Roboter Studio + Inferenz within ~3 s of opening the browser on an idle stack (idle-tick acceptance), toggle hidden, `:8769` bridge not started, leader-less fast-rehydrate; node respawn re-delivers identity without re-selection; a failed RS toggle rolls back without changing the type; a mid-teleop collision still drives the modal with the idle tick silent throughout; INFERENCE_LOADING holds for the whole eager load; stale-session notice appears only once a browser is open. The D7 German wording (selector labels, Schritt A/B, gate toasts, HomePage copy) is placeholder-quality pending review. Deferred by design: HOME ±π/2 vs ±π/4 reconciliation. (The 2026-07-22 edu6 work RESOLVED the rest of this list: `urdf_asset_id` is consumed by UrdfTwin's `URDF_ASSETS`, the DOF generalisation landed, and the reach annulus is caps-driven.)
- **Rig-validation-pending from edu6 (updated 2026-07-26 — R1/R2/R6/R9 + Q1/Q1b DONE on arm EDU6-0001; the 7-guard driver stack is LANDED, CI green, images byte-verified from GHCR at `5edb7979`; R3/R4/R5/R7/R8/R10 remain, and **A12 — „can a goal write energise a limp arm?" — goes FIRST because hand-guide's torque-off-is-safe assumption rests on it**)**: the plan §11 gates, most load-bearing first. ✅ **R1 done** — COM5, `1A86:55D3`, iSerial `5A68010132`. ✅ **R2 done** — all 7 servos register-dumped, provisioned and re-verified live; firmware 3.10; powered zero-hold held; **the servo set is MIXED by design (joints 2/3 are STS3250 model 2825, the rest STS3215 model 777)**; and the encoder question is SETTLED: the firmware computes `Present_Position = (Actual − Homing_Offset) mod 4096`, i.e. **it WRAPS** (bench datum: J5 raw 23 under a +85 offset read Present 4034), so the magnet seam never reaches our software, `/joint_states` is continuous, and the only discontinuity our code sees is our OWN tick↔angle map edge at exactly ±180° from each joint's zero — which is why the six-horn re-clock and the J2/J3/J4/J6 limit trim were both DROPPED (2026-07-25) and full ±180° windows are KEPT. ✅ **R6 done (2026-07-25)** — per-joint direction signs measured read-only on the real arm and ALL DEFAULT (+1): `EDUBOTICS_EDU6_JOINT_SIGNS` stays UNSET, the existing provisioning stands, no re-jig. Four joints (J2/J3/J5/gripper) are proven by limit-impossibility (the negated reading lies outside the URDF window, so it is physically impossible), J1 and J4 by absolute direction tests; J6's sign is deferred and FREE to flip (its window is symmetric, so no re-provision) and shows itself at the first tag-aligned grasp — the same observation as the owed `GRASP_ROLL` jaw check. Also answered: **J4/J6 CAN be hand-turned past ±180°** (J6 swept −178.1°→+179.7°), so the ±180° map-edge residual is REACHABLE, not theoretical — which is why the J6 ±90° fold and `q4 ≡ 0` are load-bearing and the boot plausibility band is a documented no-op on exactly those two joints. ✅ **The ±180° wrap test is DONE (2026-07-26, first powered act)** — the servo loop is NOT wrap-aware and drives the long way round; the fix that shipped is the driver-side torque-on edge refusal described in the driver bullet above (the ±170° EEPROM trim was measured to be the *hardware* half and stays DROPPED). ✅ **R9 done (2026-07-26)** — worst innocent torque-off excursion 107 ticks (9.4°, joint5, mechanically cross-validated) against the 400-tick band, so `EDUBOTICS_EDU6_BOOT_POS_TOL_TICKS` keeps its default; joint1/joint4/joint6 measured exactly 0 as predicted. Remaining: R5 50 Hz loop rate through usbipd (+ the never-read position P/I/D gains); R4 pinch force at `Max_Torque 150` → final overcurrent floor (now readable live — the driver publishes `Present_Load` as `JointState.effort`); R3 cube seating → effective tool length (ships 0.1724 fingertip-TCP); R7 self-collision stall-vs-damage (decides the deferred jog guard); R8 3D-twin browser smoke (π-yaw, finger animation, „Bahn" trail); R10 end-to-end scan→start→calibrate→Blockly pick→record→replay→cloud save. **`drop_at`'s release clearance is BISECTED, not baked (fixed 2026-07-26).** It releases `DROP_HEIGHT_M` (0.05 m) above the target to clear a container rim, and the old code solved `target + full clearance` as a HARD requirement — so an unreachable clearance refused an otherwise perfectly placeable target. That is the same asymmetry HIGH-5 fixed for the pickup approach (whose docstring already said the old code "refused the whole pickup/DROP even though the object was graspable"); the drop POINT was the residual it never reached. `_reachable_release_clearance` now bisects the clearance down to what is reachable, keeping the FULL clearance wherever it fits and warning in German when it shrinks; only an unreachable TARGET still refuses. Measured: edu6's „lege ab bei (Position von X)" went from **13 % → 90 % of the pick band** (`object_position` returns the GRASP height, and the release stacked 0.05 on top, landing 0.5 mm under this arm's 0.0655 m ceiling); on the OMX the outer ring stopped refusing to save **1.6 mm** of clearance (48.4 of 50 mm reachable there). `tools/generate_edu6_mat.py` still prints the second rim, but it now means "how far out you can place INTO a container", NOT a place limit. Known-accepted this round: **rig gate R7 should also cover base-swing vias**, whose near radius (0.10) sits only 10 mm outside `reach_inner_m` (0.09) — and `reach_inner_m` is consumed ONLY by the React display annulus, never enforced server-side, so nothing refuses a via inside it. Also `sim_arm._GRASP_CAPTURE_RADIUS_M` (0.06) spans edu6's whole radial band (0.42× on the OMX), so the sim reports HELD for a close up to 60 mm off target — sim-fidelity only, and it carries no object identity so it cannot grasp the wrong object. Blockly `WORKSPACE_BOUNDS_M` (±0.40) stays static — merely permissive for edu6 (IK refuses with the German „Arbeitsbereich" anyway); the 60 mm hover over a 30 mm cube tops out ~51 mm at the annulus sweet spot (the +110° relief minus the 12 mm-longer tool; `_solve_grasp_and_approach` bisects gracefully — a ~40 mm riser is the 1:1 physical lever if ever wanted); `edu6_arm_records` has no fetch-by-serial route yet (the student-connect verify uses the servo EEPROM itself; the route lands with the first RMA need). **R7 now also decides the zone-inflation question** — does a ~34 mm gripper-housing overhang past the 50 mm inflation produce damage or a harmless overload stall (see the no-go-zone bullet)? And **expect roughly the outer 2 mm and inner 2 mm of the pick band to REFUSE a grasp** („Kein Platz, um von oben anzufahren", 19 of 704 swept radii) — that is the 2026-07-26 clearance floor working as designed, not a fault; r ∈ [0.05, 0.19] is silent. Residual: a `move_above` clearance refusal inside „Solange sichtbar" is swallowed but the tag is NOT skipped (`_skip_tag` lives in `perception_blocks`; `motion` importing it would be circular), so that loop ends via the existing 3-pass stall guard rather than cleanly — `grasp_object`, the Beginner-mode loop body, does skip.
- **Rig-validation-pending for `edu1_studio` („Edu:1", added 2026-09-05) — NOTHING is hardware-validated; every physical number in the profile is DERIVED FROM CAD or ASSUMED.** The software chain is complete and green (solver, box model, catalog, driver spec, scan, cloud tag, React asset), so what is open is measurement, not code. Gates, most load-bearing first:
  - **E1 — servo bus layout.** Assumed: ids **1..6 in joint order, the claw = id 6**; models STS3215 on 1/4/5/6 and STS3250 on the high-load shoulder + elbow 2/3 (the mixed set the probe already accepts, so a swap between the two is harmless — a THIRD model is not). Wrong ids mean the boot probe refuses in German, which is the safe failure; wrong ORDER means every joint drives the wrong axis and the probe cannot see it. **Check the order first.**
  - **E2 — the USB bridge really is `1A86:55D3`.** If it is, edu6 and Edu:1 are USB-indistinguishable and the prober's exact servo COUNT is the only discriminator (already implemented). If it is NOT, `ARM_USB_IDS['edu1']` and `_FEETECH_BYID_MARKERS` need the real ids and the two families become separable one layer earlier.
  - **E3 — per-joint direction signs.** Default all `+1` (same starting assumption edu6's R6 later CONFIRMED). Overridable per rig via the driver's joint-signs env knob with no image rebuild. **Flipping a sign requires re-provisioning**, because the EEPROM window is written sign-aware.
  - **E4 — EEPROM provisioning. ⚠ RE-OWED 2026-09-10 ON EVERY ALREADY-PROVISIONED ARM:** joint4 was widened ±90° → ±115°, which moves its EEPROM window **1024…3072 → 740…3356 ticks**, and the boot probe verifies that window as a fingerprint — so any Edu:1 provisioned before that date now refuses to boot with „nicht provisioniert" until it is re-run. (Both ends stay inside 0…4095, so nothing saturates.) `tools/edu6_provision.py --arm edu1` must be run once per arm on the jig BEFORE it can boot at all: the driver's boot probe verifies the exact `Min/Max_Position_Limit` window this writes. Also unmeasured: the arm/claw `Max_Torque` floors, which are inherited from edu6 (800 / 150) and are a PINCH-FORCE decision, not a copy-paste one.
  - **E5 — the grasp numbers.** Simulated against the shipped claw STLs, a 30 mm cube blocks the jaws at ≈0.25 rad with the end-effector origin 0.090–0.115 m above the table; the catalog therefore commands 0.10 with a 0.10 held-margin (threshold 0.20). All three want a bench check, and they are one catalog edit + image rebuild apart.
  - **E6 — HOME and the boot-home glide.** `(0, 0.64, 1.48, 0.90, 0)` was chosen by measurement against the box model (see `CLAUDE.md`), but "the arm stands up over its base" is a claim about a REAL arm's balance and cable routing. The glide refuses ~1/1200 limp-collapse starts by design.
  - **E7 — the `GRASP_ROLL_DEG` 90° jaw convention** on the real claw, exactly as for edu6: no runtime guard exists, and it shows itself at the first tag-aligned grasp.
  - **Fixed on the way in, and it was an edu6 bug too:** the touch-off's per-tap verticality gate hardcoded FK rotation column 0 as the tool axis. That is the OMX's convention; both Feetech arms point their tool along ∓z, so the gate measured **90°** against a 12° limit and rejected EVERY correct vertical tap with „Greifer steht schräg (90°)". Nobody had reached the touch-off on an edu6 yet, so it had never been seen. The axis now comes from the solver (`ik.approach_axis_local`), all three read 0.0° at a vertical pose, and `test_touch_off_approach_axis.py` pins both the per-solver declarations and the old naive column.
  - **E8 — touch-off with the claw CLOSED.** The TCP is the closed fingertip, and this claw ROTATES: its tip is 86.25 mm below the EE origin closed but 68.8 mm at 0.9 rad open. „Tisch vermessen" taught with an open claw measures the table ~17 mm too low. The wizard now SAYS so — `TableTouchStep` shows „Greifer ganz schließen" whenever the manifest carries `tool_tip_tracks_gripper`, which only this arm does — so what is left on this gate is confirming the 17 mm on the bench, not writing the sentence.
  - **E9 — joint4's ±115° range: the ONE thing in this change that software cannot check (added 2026-09-10).** joint4 is now declared ±115° (`2.0071` rad) instead of ±90°, on the OWNER'S STATEMENT that the hardware reaches 115° to each side. Nothing in this repo can verify that, so treat it as a CEILING: never widen past it, and confirm on the bench that BOTH extremes are reachable without the wrist striking link3 or the claw fouling the forearm, at the `Max_Torque` floors E4 leaves in place. What the widening buys, measured through the real solver: **902 → 2068 reachable cells** of a 5751-cell grid and a strict-vertical TCP ceiling of **0.100 m → 0.195 m**. What it does NOT change, also measured: the arm's POSTURE (the elbow-down branch becomes legal over part of the workspace but is still never selected — 23359/23359 solved targets come back elbow-up), HOME (unchanged, still bound by joint2 at 0.640 rad), the box model's SOUNDNESS (400/400, 2000/2000, and 20000/20000 sampling only the newly opened bands), and the reroute grid (36/72 at both windows). Still open on the bench: whether the taller cruise the 0.195 m ceiling now permits is worth taking — the reroute constants were deliberately left at their ±90° values, so no student sees the extra height yet.
  - Known-accepted, not gates: `reach_inner_m`/`reach_outer_m` (**0.07/0.35** since 2026-09-10, was 0.09/0.35) are a DISPLAY annulus, never enforced server-side, same as on edu6. The RULE behind them changed with the joint4 widening — the ring is now the INTERSECTION of the grasp plane and the hover the catalog asks for, which is why the outer stayed at 0.35 while the grasp plane alone now reaches 0.374.
  - **KNOWN-ACCEPTED (measured 2026-09-05 on the shipped meshes, exact box/triangle intersection) — the closing sweep is a SCOOP, and nothing models it.** At the catalog grasp (EE origin 101.25 mm, TCP 15 mm, 30 mm cube under it) the OPEN claw is not straddling the cube at all: its lowest point is at z = 32.4 mm, 2.4 mm ABOVE the cube's top face, minimum blade-to-cube distance **35.4 mm**. Each blade then rotates about its own pivot and enters the cube's height band from above and outside — the fingertip starts at |y| = **54.8 mm** (blade extreme 56.7 mm) and first touches the cube's VERTICAL SIDE FACE at q = 0.2474 rad. **Consequence: a second object is swept aside before the primary is even touched.** The swept set never descends below z = **14.7 mm**, so the usable rule is: anything TALLER than ≈14.7 mm whose footprint comes within ≈**52 mm** of the grasp point along the jaw (y) axis and within about −12…+11 mm along the jaw-width (x) axis is struck by a closing blade. For a second 30 mm cube that means its CENTRE anywhere with |y| ≤ 66.7 mm — **more than two cube-widths away is still swept**. Nothing checks this: `path_guard` judges TRANSIT, not the closing sweep, and no-go zones are not consulted during a grasp. It is a classroom-scene layout constraint, not a code gate.
  - **KNOWN-ACCEPTED — the held-detection margin is thin but POSITIVE, and the real edge is a cliff, not the margin.** Threshold = commanded close 0.10 + `grasp_held_margin_rad` 0.10 = 0.20 rad; measured blocking angle is a flat **0.24737 rad (14.17°)** for EE-origin heights 85…110 mm — constant, because contact is on the cube's vertical side face, which the blade profile reaches at the same jaw angle whatever the height — giving **+0.0474 rad (+2.71°)** of margin. It degrades slowly above 110 mm (+0.0334 rad at 115, worst **+0.0298 rad / +1.71°** at 116) and is never negative, so a false HELD is not reachable. **At H ≥ 117 mm the jaws close PAST a 30 mm cube without touching it at all** → `check_grasp_held` reports MISS, which is the fail-safe direction but silent. A touch-off that reads the table ~16 mm too HIGH therefore turns every cube grasp into a silent miss; note E8's open-claw error goes the OTHER way (~17 mm too LOW), which drives the claw into the table instead. Both are bench-settleable at E5/E8. Raw and decimated meshes give bit-identical blocking angles, so this is not a mesh-resolution artefact.
  - **KNOWN-ACCEPTED — `EDU1_LINK_BOXES` is SOUND but has almost no slack, and the 0.1 mm outward-rounding rule is what makes it sound.** All 36 faces re-checked against the FULL-RESOLUTION meshes: **zero negative headroom**. The tightest is box 5 (link5 + end_effector + both fingers, swept over the whole claw band) z-high, at **+0.0090 mm — 9 µm** — the true swept maximum is 86.8910 mm, attained by `right_finger` at jaw q = 0.13985, against a box top of 86.9000. **TEN faces sit at exactly 0.000 mm** (base_link z-lo, link1 x-lo/x-hi/z-lo, link2 y-lo/y-hi, link3 x-lo/x-hi/y-lo, link5+claw z-lo) — they touch, none is violated; they are zero because the raw extent is already an exact multiple of 0.1 mm. **Tightening the outward-rounding rule would make the model OPTIMISTIC**: the decimated meshes the boxes were derived from fall INSIDE the raw ones by up to 0.0133 mm (0.0110 mm over the 36 faces themselves), and the 0.1 mm rule is what absorbs that with ≥0.089 mm to spare. Every one of the ten zero-headroom faces happens to have zero decimation deficit, so the derivation never produced a box smaller than the raw geometry — but that is luck the rounding rule is not relying on.
- **Adding the Edu:1 CHANGED FOUR THINGS AN edu6 RIG WILL NOTICE, and `CLAUDE.md`/the changelog's "the server needed no branching" headline is about the SERVER only — the SCAN path is where edu6 behaviour moved.** All four are deliberate and all four are improvements; they are written down because "edu6 is unchanged" is otherwise the natural reading. Measured by running the same probe against `git archive HEAD` and the working tree (`scratchpad/scan_probe.py`, 336 leaves compared):
  1. **A 6-servo bus is no longer `partial:6`.** An edu6 whose GRIPPER servo has dropped off the chain used to say „Nur 6 von 7 Servos antworten — bitte die Steckverbindungen … prüfen". It now returns `edu1_arm_found` and says „Es antworten genau 6 Servos, ausgewählt ist aber „EduBotics 6-Achs …" … entweder ein „Edu:1"-Arm … oder der 6-Achs-Arm, dem das siebte Servo fehlt". Strictly more informative and irreducibly ambiguous — those ARE the same six servos — but a technician who knows the old sentence will not see it again.
  2. **`partial:N` changed MEANING**, from "how many answered" to "how many CONTIGUOUS from id 1". A bus answering 1,2,3,5,6,7 used to report `partial:6` and now reports `partial:3`. The new number is the more useful one (everything past a hole is unreachable anyway) but it is NOT the same statistic.
  3. **An 8-or-more-device bus is now REFUSED.** The old prober only pinged ids 1..7, so a bus with an extra device on it identified as a healthy edu6 and the stack came up. It now returns `bus_too_long:N` with its own German sentence naming the extra-device cause. Rejecting is right; the intermediate wording („Nur 8 von 7 Servos antworten") was not, and is fixed.
  4. **The two USB-presence sentences became family-GENERIC** („ein EduBotics-Arm" instead of „EduBotics 6-Achs"). That path only sees VID:PID, and one adapter now serves two arms, so the specific wording was a coin flip stated as fact.
  Everything else an edu6 rig touches IS byte-identical to HEAD, and that was verified by execution rather than by reading: the server-side profile probe (30 dimensions per profile including a 136-target IK sweep, the box model, the catalog, the path_guard constants and the velocity floor) reports **0 differences** for `omx_full`, `omx_follower` and `edu6_studio`, and the driver's own constants — read inside a `ros:jazzy-ros-base` container under `EDUBOTICS_ROBOT_TYPE=edu6_studio`, which is the only way the `if IS_EDU1:` override is actually executed — report **0 differences** across 11 keys including every EEPROM position window and the boot-home floor decision.
- **The Edu:1 shares a Contract-B point width with the OMX (7), which is new.** Before it, width and arm were in bijection. `workflow_trajectories.robot_profile` (migration 039) is now the ONLY thing separating an OMX recording from an Edu:1 one, on both the cloud validator and the React replay gate. Anything that "simplifies" the profile→width map into a length lookup silently re-opens cross-arm replay.
- **Some n=6 test coverage is still STUB-based, and that is where an edu6-only bug hides.** `test_dof_n6.py::_FakeIK6` is a WIDTH stub with no `solve()` at all, so the slice-2a…2d tests prove plumbing and no convention or geometry. Two live defects have already been found in exactly that gap — the Cartesian-jog wrist mirror (2026-07-25) and the profile-blind reroute geometry + `plan_safe_route` roll fallback (2026-07-26) — both edu6-only, both invisible to a fully green suite. Slice 2e drives path_guard and SimArm through the REAL `Edu6IKSolver` + the real registry profile (the jog was already covered by `test_jog_cartesian_keeps_the_wrist_with_the_REAL_edu6_solver` from the 2026-07-25 batch), and every guard in it is mutation-verified. **Crucially it also covers the LAST MILE** (`test_workflow_manager_stamps_every_profile_field_onto_the_REAL_ctx`): path_guard reads its geometry off `ctx`, and the only thing that puts it there on a real run is `WorkflowManager.start`'s `WorkflowContext(...)` — a one-character typo in a `getattr` key silently reverted the whole fix with the entire suite green, and that hole was pre-existing for `velocity_limit_rad_s`/`grasp_held_margin_rad` too. **Add a row to that test when you add a profile stamp.** The one sim test that used the stub for GEOMETRY was migrated to the real solver + the real edu6 gripper band (it had published an OMX-style negative close of −0.5, outside edu6's legal 0…1.75 band, and passed only because the stub's constant `fk` pretended the arm stood on the cube). `_FakeIK6` is deliberately KEPT in `test_path_guard_segment_blocked_slices_n_joints`: there the stub is the right tool because it *asserts* `fk`/`link_points` receive exactly 6 joints, which the real solver cannot do (it returns `None` for a short vector and `segment_blocked` reads `None` as "not blocked", so swapping it in would weaken the check to a tautology). Still stub-only: `SimPerception`/sim-scene and the trajectory n=6 legs. **When an n=6 test could assert a convention, use the real solver; keep a stub only where the stub itself is the assertion — and when a value has to travel from the profile to a consumer, test the seam, not just both ends.**
- **arm64 runner fallback plan**: if `ubuntu-24.04-arm` leaves the free GHA tier, add pinned `setup-qemu-action` and expect 5-8× slower arm64 builds + FP edge cases.
- **`base-digest-check` is `continue-on-error`** — upstream `robotis/*` base drift only warns.
- **Dead code to prune eventually**: `docker/physical_ai_server/patches/kdl_parser_py/` (tracked, unused), `docker/physical_ai_server/overlays/` (orphan pycache, incl. `fabrik_*` remnants of a pre-analytical-IK attempt), `gui/dist/` (stale build output), `physical_ai_server/video_encoder/` (orphaned package, zero references), `workflow/projection.py::project_base_to_pixel` + `workflow_manager._BroadcastShim` (kept-for-tests only), `tools/dfine_finetune.md` + `tools/perception_eval.md` (historical, already ⚠-bannered). On the Orange Pi specifically, `pi_agent/constants.py` carries SIX symbols with **zero readers anywhere in `pi_agent/`** — `PORT_CAMERA_INGEST`, `PHONE_CAM_ID`, `CAMERA_BRIDGE_ROLES`, `BAUDRATE`, `LEADER_SERVO_IDS`, `FOLLOWER_SERVO_IDS`. Every apparent reader resolves against the WINDOWS twin instead (`tests/test_phone_camera.py` and `tests/test_camera_bridge.py` both open `from gui.app import … constants`), so the Pi copies are unread by production AND by test; `LEADER_SERVO_IDS`/`FOLLOWER_SERVO_IDS` are the starkest at two occurrences each repo-wide — the two definitions, no readers on either platform. **Two of the six are deliberate and should NOT be pruned blind**: `PORT_CAMERA_INGEST` says so in its own comment (the Pi runs the in-container `usb_cam` path, so nothing feeds `:5557` — kept so the port map stays complete), and `CAMERA_BRIDGE_ROLES` is the only statement of the cam_id↔role index on the Pi. **Do not extend this list to `DEFAULT_ARM_FAMILY`**: it is production-unread but THREE test methods in `pi_agent/tests/test_constants.py` read it, one existing precisely to assert it stays DERIVED from `ROBOT_PROFILES` rather than restated — deleting it removes the guard against a second id→family source of truth.
- **Stale comments/docs that actively mislead** (fix when touched): `docs/deploy/DEPLOY.md` + `PIPELINE.md` now carry a DEPRECATED banner pointing at Rule §6 (their pre-GHCR steps remain below it — delete or rewrite when convenient); `wsl_rootfs/README.md` was corrected to `[boot] command` + `ROOTFS_VERSION`; `docker/BASE_IMAGE_PINNING.md`'s M14 section describes the dead overlay model (and notes an un-actioned upstream server-base 0.8.3 bump); the stale "gravity compensation" comments (`docker/open_manipulator/Dockerfile`'s leader block, `collision_monitor.py`'s header re `/collision_flag` consumers); `omx_f_follower_ai.launch.py`'s header line-number self-references.
- **Triaged low-priority bugs (2026-07-03 audit, unfixed)**: `inference_manager.get_saved_policies()` risks an UnboundLocalError (`snapshot_folders` assigned under an `if`, consumed unconditionally); `data_manager._check_dataset_exists` does an unbounded `requests.get` on the node-DEFAULT callback group every collection tick (an HF outage can wedge the Stopp button) and silently `rmtree`s a malformed local dataset with English-only logging; `register_huggingface_token` waits 1.5 s vs whoami's 8 s (cold-network false negatives at Schritt D); `wait_until_held`/`grasp_held` read „held" from an OPEN gripper (documented in-code); `jetson_agent/rosbridge_proxy.py` has zero test coverage.
- **THE P0 PASS LANDED 2026-08-06 on branch `security/p0` (5 commits, unpushed, unreleased).** Owner-approved scope: close the rosbridge control plane by same-origin nginx proxy + Origin allowlist AND dropping the host publishes; confine every client-supplied dataset path while KEEPING the Benutzer roster; wipe the WebView2 profile at spawn; fail upload privacy safe; patch GDPR erasure honesty only. Suites: `robotis_ai_setup` 890 → **984** OK(skipped=6), `cloud_training_api` 230 → **246** OK, `pi_agent` 575 OK (untouched), `physical_ai_server` pytest 901 passed/4 skipped, React 498 → **501**, eslint clean, `nginx -t` OK. **Two findings the audit had NOT made**: `rosbridge_server` 2.7.0 OVERRIDES tornado's same-origin default (`def check_origin(self, origin): return True`, verified in the installed package and at four upstream tags) and ROS 2 deleted the `authenticate` parameter in 2021 — so the hole was LIVE, not latent, and the nginx proxy was the ONLY mechanism available; and `web_video_server` sets `Access-Control-Allow-Origin: *` on the very `MultipartStream` path the app's `<img>` uses, which nginx was MEASURED forwarding, so cross-origin JS could read the classroom camera (fixed with `proxy_hide_header`). An adversarial self-review then found four more defects in the pass's own earlier commits — a naive `os.path.join` surviving in the MORE exposed `handle_browse_with_target_check` twin (the confined sibling was the one React never calls), a reflected XSS via `handle_stream_viewer`'s raw `topic` echo on `http://localhost` (the exact Origin the `/rosbridge` gate trusts, since narrowed to an EXACT `/video/stream`), a SECOND unconfined `rmtree` reachable through the unsanitised `user_id` component of `_save_repo_name`, and a self-inflicted availability regression where confining to one root refused the legitimate `POLICY_MODEL_PATH`. **Reaches a rig only via an image rebuild (nginx.conf + the COPY-wholesale server package) PLUS an interfaces rebuild for the `.msg` PLUS a GUI/installer build for `webview_window` — nothing below is live for a student until a release ships.** Residuals, each deliberate: the Supabase refresh token is deleted locally but never revoked server-side; a handover on a STILL-OPEN window is untouched (**that wording badly understated it — see the adversarial-review entry below; the Stop→Start half is now FIXED**); `delete_my_account` still deletes nothing (it is now honest about that in German); and repos in a student's own HF namespace remain unerasable by the platform, which is what `docs/plans/2026-08-06-hf-org-namespaces.md` specs out.

  **CORRECTIONS TO THIS ENTRY, made 2026-08-07 by the P0 FOLLOW-UP (7 commits on the same branch).** Three claims above were overstated or plainly wrong. They are corrected rather than deleted, so the shape of the mistake stays visible:
  - **„Blockly crash-recovery no longer survives a process restart" was not a residual, it was a REGRESSION — and it is gone** (struck from the residual list above). `wipe_browser_profile` was an unconditional `rmtree` of the WebView2 user-data folder, which is where localStorage AND IndexedDB live: it destroyed the autosave AND every `MACHINE_SCOPED_KEYS` entry (`edubotics_robotType`, whose own comment says clearing it costs the next student an arm re-scan; `edubotics_audio_muted`; the four dock keys; `edubotics_urdf_open`), and it fired for a student who merely closed the window and clicked „Web-Oberfläche öffnen" again mid-lesson. Replaced by an SPA boot scrub: the GUI stamps `?fresh=<nonce>` and `src/utils/bootScrub.js` runs `clearStudentScopedStorage()` + `clearSupabaseSessionKeys()` before Redux or supabase-js read storage. The non-roaming `storage_path` STAYS — it independently fixes the profile living in the ROAMING `%APPDATA%\pywebview`.
  - **„confine every client-supplied dataset path" did not.** `PhysicalAIServer.get_training_info_callback` built `get_weight_save_root_path() / request.train_config_path` and `open()`d the result, unconfined, in the very file that commit edited. It was missed because it is a MODEL path, not a dataset path — the sweep's own framing hid it. Fixed with a new `dataset_paths.safe_under()`.
  - **The browse confinement BROKE „Modellpfad auswählen", and there were THREE disagreeing paths, not two.** `dataset_paths.model_root()` derived its browsable root from `TrainingManager.get_weight_save_root_path()`, which named the pip site-packages `outputs/train` — a directory NOTHING writes to; React's `POLICY_MODEL_PATH` named a third path inside the vendored lerobot tree the image build `rm -rf`s; and the DOWNLOADER has written to `~/ros2_ws/outputs/train` since v2.5.0, deliberately, for exactly that reason. Before confinement the mismatch merely opened an empty modal the student could navigate out of; afterwards it was a German security refusal with nowhere to navigate. All three now read `dataset_paths.MODEL_ROOT_RELATIVE`, fenced cross-language by `physical_ai_server/test/test_model_root_agreement.py`.
  - **Three guards named a behaviour without fencing it, and are now fenced.** The HF-namespace refusal — the headline of the handover commit — had ZERO tests: replacing it with `if False:` left every suite green. Same for two `routes/teacher.py` behaviours: treating `RepositoryNotFoundError` as SUCCESS (already-gone is the desired end state, and folding it into the generic handler would tell a teacher who re-runs a deletion that data remains), and reporting a datasets-registry query failure. And the GDPR honesty fix never reached a human: `StudentRow.handleDelete` discarded the response and showed an unconditional „Schüler gelöscht", so the whole of 93e15fc stopped at the wire.
  - Also landed in the follow-up: the Orange-Pi parity gap below is CLOSED, and the graceful-close / `self.running` pair below is FIXED.
- **THE P0 FOLLOW-UP LANDED 2026-08-07 on `security/p0` (7 further commits, unpushed, unreleased).** It fixed two regressions the P0 pass itself introduced (the profile `rmtree`, the „Modellpfad" refusal), confined the one client path that pass missed, ported the whole rosbridge gate to the Orange Pi, fenced three unfenced guards, surfaced the GDPR response in the UI, and fixed the shutdown pair. Suites: `robotis_ai_setup` 986 → **1060** OK(skipped=6), `cloud_training_api` 246 → **253** OK, `physical_ai_server` pytest 901 → **913** passed / 4 skipped, React 50/501 → **52/526**, `pi_agent` 575 OK (untouched), eslint 0, `nginx -t` clean on all three configs, `docker compose config` clean on student+gpu / opi / the pi_agent twin (still `cmp`-identical). **Two commit messages carry a wrong test COUNT** — `82884fd` says „987 → 993" (really 1000) and `fccd472` says „1000 → 1013" (really 1012). Not amended, per the standing rule against rewriting commits; the numbers above are the measured ones.
- **An adversarial review of the P0 FOLLOW-UP found three more holes (2026-08-07). Two are FIXED; the rest are recorded here.**
  - **[FIXED] The Blockly CROSS-TAB CLIPBOARD survived the handover scrub — a regression of the boot-scrub commit.** `@mit-app-inventor/blockly-plugin-workspace-multiselect` writes the student's COPIED BLOCKS to localStorage as `blocklyStashMulti` / `blocklyStashConnection` / `blocklyStashTime` and reads them back on Ctrl+V, so student B could paste student A's program. The `rmtree` this replaced deleted them. **Three independent reasons no existing guard could see it, and the fix answers all three:** the writer is a DEPENDENCY and `sessionScope.test.js`'s coverage scan walks `src/` with `node_modules` excluded; the keys do not begin with `edubotics`, and that scan's `isEdubotics` filter judges only keys that do; and the feature is ON BY DEFAULT (`useCopyPasteCrossTab_ = true` in the constructor, disabled only by an explicit `multiselectCopyPaste.crossTab === false`, which `BlocklyWorkspace.jsx`'s `ms.init({})` never provides — verified in the installed dist), so nothing in our source mentions it. All three keys are now in `STUDENT_SCOPED_KEYS`, and `bootScrub.crossTab.test.js` READS NODE_MODULES to assert the plugin still writes exactly those names, still writes them to localStorage, and still defaults cross-tab to on — the only test in the suite that does, deliberately, because the invariant is a fact about the dependency. **This falsifies the earlier claim that the storage registry is „complete":** it is complete for `edubotics*` keys reachable from `src/`, which is a much smaller statement about a much smaller surface.
  - **[FIXED] „Umgebung stoppen" left the browser window alive, so the next „Umgebung starten" handed the next student the previous one's window AND session — on the primary lifecycle path.** `_stop_environment` tore down the camera bridge, phone server, `:8769` bridge, containers and keepalive but never called `webview_window.destroy_all()` (it had exactly TWO call sites, `_on_close` and the updater). `_do_start` ends in `_open_webview()`, which minted a fresh `?fresh=<nonce>` URL that `open_student_window`'s live-child short-circuit then DISCARDED while returning True — so the Protokoll logged „Web-Oberfläche wird im EduBotics-Fenster geöffnet." while no document loaded and the boot scrub never ran. **PRE-EXISTING, not caused by the scrub:** the profile `rmtree` sat BELOW the very same short-circuit and never ran on this path either. This is what „a handover on a STILL-OPEN window is untouched" was actually describing, and that wording badly understated it — it is the two buttons the product is operated with, not an edge case. `_stop_environment` now closes the child BEFORE the containers go down, so the graceful WM_CLOSE still has a live rosbridge for JogPanel's unmount re-torque.
  - **[NOT FIXED, deliberate] An unwritable sessionStorage makes every reload re-scrub.** If the latch cannot be written, `useVersionCheck`'s reload carries the same `?fresh=` and scrubs again — so a student who signs in is signed out at the next image update, for the life of that install. Left as is because every alternative is worse: a localStorage latch SURVIVES THE WINDOW and would suppress the scrub for the NEXT student (fail-unsafe, the one direction this may never fail in); a module-level variable dies with the document and so cannot span the reload that is the problem; and `window.name` would add a persistence surface outside the registry to defend a state that needs localStorage WORKING while sessionStorage FAILS — both are Web Storage and normally fail together, so it may not be reachable at all. **Reachability is UNPROVEN**; settle it by disabling Web Storage on a Windows rig and watching whether localStorage still works.
  - **[UNPROVEN] Two live windows on one profile could re-persist A's session into B's scrubbed window.** B scrubs → A's surviving window's `autoRefreshToken` fires → auth-js rewrites `sb-<ref>-auth-token` into the SHARED localStorage → B's next reload carries the same nonce, hits `already`, and `getSession()` restores A. Needs two live children — reachable only by killing `EduBotics.exe` with the web window open and relaunching, since `destroy_all` now runs on BOTH normal exit and „Umgebung stoppen" — and needs WebView2 to share localStorage across two host processes with the same `storage_path`. **Settle it on a rig:** kill EduBotics.exe from Task Manager with the web window open, relaunch, open a second window, check whether the two share localStorage.
  - **[SCOPE GAP, by design] Nothing Pi-side stamps `?fresh=`**, so an Orange-Pi rig never boot-scrubs. That is correct and must stay: the Pi's browser is the student's OWN device, so no shared browser profile exists to leak, and the Pi is CONCURRENTLY multi-tenant — a scrub there would destroy other students' live sessions. The teacher web build also no-ops, for the same reason. Both are the existing „do NOT twin the Windows session-lifecycle model onto the Pi" rule.
  - **What the review CONFIRMED, so it need not be re-derived:** the import-order guarantee holds against the real Rolldown bundle executed under instrumented storage (scrub ops 0-14, then taskSlice at 16, trainingSlice at 20, supabase at 21); auth-js 2.103.1 writes only `storageKey` and `${storageKey}-code-verifier` into `storage` (the `-user` key goes to `userStorage`, which is null here), so the `sb-` pattern is complete for this version; and a reload keeps the student signed in.
- **The IMAGE and the COMPOSE are now VERSION-COUPLED on Windows, so `EDUBOTICS_IMAGE_TAG` is no longer a one-variable rollback (2026-08-07).** `docker-compose.yml` ships with the INSTALLER (`{app}\docker\`, `constants.COMPOSE_FILE`) while `EDUBOTICS_IMAGE_TAG` pins only the IMAGES, so the two can come from different releases — and since the rosbridge move that matters. Pinning to a pre-fix release gives **new compose (no `:9090`/`:8080` publishes) + old nginx (verified: ZERO occurrences of `rosbridge` in `nginx.conf` at `ecd99e7`) + old SPA (`localRosbridgeUrl` returning `ws://<hostname>:9090`)** = a robot the browser cannot reach at all. **The rollback is two-step: install the older `.exe` AND pin the tag**, or pin nothing and let the baked `versions.env` match the installer. The reverse direction is safe — an OLD compose with a NEW image merely publishes two ports nothing uses. The **Orange Pi is unaffected for AVAILABILITY** (its compose keeps the publishes and the agent self-repairs the compose to the release it runs), but pinning an old `-opi` tag silently drops the new Origin gate: a security rollback, not a neutral one. Documented in `CLAUDE.md` beside both places that call the pin a rollback. **No code changed for this** — it is an owner-decision documentation item.
- **Residuals created or left open by the P0 follow-up (2026-08-07), each deliberate.**
  - **The boot scrub cannot revoke server-side.** `utils/bootScrub.js` deletes the persisted Supabase session locally; the refresh token stays valid at Supabase until it expires. Unchanged from the `rmtree` it replaced. It deliberately does NOT call `supabase.auth.signOut()` — that belongs to `utils/signOut` and to one non-test file, and it is a network round trip on a boot path, on behalf of a student who has already walked away.
  - **The scrub's once-per-window latch rests on a sessionStorage lifetime this repo cannot prove.** sessionStorage surviving a reload is spec; sessionStorage dying with a WebView2 window is what `edubotics_workshop_autosave_session` already assumes but nothing measures. The NONCE is the mitigation and it fails in the SAFE direction: a new spawn carries a new nonce and scrubs regardless of what survived, so the only exposure is a redundant scrub, never a skipped one. Worth confirming on a rig anyway.
  - **The WebView2 HTTP cache and cookies are no longer cleared.** The `rmtree` took them; the scrub does not. Assessed as not identity-bearing — the app sets no cookies, Supabase persists to localStorage, and `docs/KNOWN-ISSUES.md`'s own audit of the storage surface found zero raw IndexedDB / cookies / Cache API / service workers / OPFS outside the registry.
  - **`safe_under` deliberately ACCEPTS an absolute path that lands inside the root.** The first draft refused absolutes outright „because the parameter means relative"; mutation testing killed that claim (deleting the check left the suite green — `confine` already refuses every ESCAPING absolute), and the only input the two spellings disagree about is an absolute path INSIDE the root, which the file browser legitimately hands back. Refusing it would have been the „Modellpfad" defect in miniature. The rule is: judge where a path LANDS, never how it is spelled.
  - **`_post_close_to_pid`'s `sys.platform != "win32"` early return is fenced STRUCTURALLY, not behaviourally.** Deleting it is uncatchable off Windows — the next statement is `ctypes.WinDLL` inside a `try` whose `except` returns the same 0. Same shape as `_read_machine_id`'s platform guard, and it gets the same treatment: `test_shutdown_teardown.py` pins it as the FIRST statement of the function.
  - **The GDPR warning is still a toast.** A partial erasure a teacher must act on arguably deserves a persisted record; that is a teacher-dashboard feature and belongs with the org-namespace work that makes erasure possible at all.
  - **`get_training_info` has no UI caller.** `useRosServiceCaller.js` exports `getTrainingInfo` and nothing in `src/` calls it, so the service's only client today is the wire. It is confined either way; recorded because „React calls it" would be the wrong mental model for whoever touches it next. **That is also why its `NameError` survived to 2026-08-31**: the success message interpolated an unbound `train_config_path` (only `request.train_config_path` was ever bound), the raise landed *after* `response.success = True`, the outer handler swallowed it and flipped success to `False` — so the service could never return success, invisibly, because nothing called it. Fixed, with a test that the success path returns `success=True`.
- **THE v2.15.0 PASS (2026-08-31 / 2026-09-01, on `main`, unpushed, untagged).** Three work items on one working tree, under the same owner-stated threat model as the P0 pass (each student on their own machine, all of them on the SAME shared Windows account, students will not attack each other — so handover CLEANLINESS is the priority and "a malicious student could…" is out of scope). **(1) A P0 security fix round**: `ControlHfServer` `mode='delete'` refused at the wire (zero callers, irreversible if wrong); the upload namespace-guard refusal stops echoing the namespace; `get_training_info_callback`'s `NameError`; a `return` inside a `finally`; a vacuous non-str-refusal test made discriminating; the two Origin checks (nginx map vs `_origin_allowed`) made genuinely identical on scheme AND port, plus a lockstep test that had been comparing hostnames only by construction; the GDPR erasure route reordered reversible-first with an honest `attempted == 0` branch and migration **038**'s `ON DELETE CASCADE`; `german_detail_lint.py`'s classifier fixed so transliteration runs on every scanned string; the `-user` Supabase-key sweep fenced; and four stale comments that asserted the opposite of the code. **(2) The student login gate** — see `CLAUDE.md`'s own bullet. **(3) The „Arme scannen" confirmation dialog.** Measured suites at the end: `robotis_ai_setup` **1160** OK(skipped=6), `cloud_training_api` **279** OK, `physical_ai_server` pytest **913 passed / 4 skipped**, React **55 files / 563 tests**, `pi_agent` 575 OK + `jetson_agent` 30 OK (both untouched), eslint 0 warnings, `german_detail_lint` exit 0, `compileall` clean on all seven CI roots under Python **3.11** as well as 3.14, `nginx -t` clean on both configs (opi rendered through the shipped envsubst filter), the two `docker-compose.opi.yml` twins still byte-identical (`0c423480cb6c4481957b5215eee7460f`). **Nothing here reaches a rig until a release ships** — it needs an image rebuild for the server + manager and a GUI/installer build for `gui_app`/`webview_window`. Unlike the P0 pass it needs no `.msg` semantics change: `TaskInfo.msg`'s edit is **comment-only** and `bool private_mode true` is byte-identical.
- **RESIDUALS CREATED OR LEFT OPEN BY THE v2.15.0 SECURITY + LOGIN-GATE PASS (2026-08-31 / 2026-09-01), each deliberate.**
  - **A teacher deleting a Blockly-only class now sees one RED toast per student.** `DELETE /teacher/students/{id}` answers `hf_erasure_complete: false` with `hf_failures: []` when it attempted ZERO HuggingFace deletions, and `StudentRow.handleDelete`'s `=== false` branch renders the German `detail` as a 15-second error toast instead of the green „Schüler gelöscht". That is the honesty fix working — it is an instruction to go and check another site — but a teacher deleting a class of 30 sees 30 red toasts, and in a classroom where most students only used Blockly that is the COMMON case, not the edge one. **If it proves unacceptable in the field the fix is a distinct SPA branch for `hf_erasure_complete === false && hf_failures.length === 0`, never a return to claiming an erasure that did not happen.** Do not loosen `=== false` to a truthiness check either: an older Cloud API sends a bare `{ok: true}` and `undefined === false` is correctly false.
  - **The Railway parallel-deploy race is real and unfixed.** `railway-deploy-cloud-api.yml` (W2) and `railway-deploy-teacher-web.yml` (W3) both fire on push-to-main with no cross-`needs:` and separate concurrency groups, so the new SPA can briefly talk to the OLD API and vice versa. For THIS change set the window is benign in both directions (new SPA + old API → the bare `{ok: true}` does not trip `=== false`, so the teacher sees „Schüler gelöscht" exactly as before; old SPA + new API → the old SPA has the same `=== false` gate and reads `res.detail` first, so the new 503 and zero-attempt bodies render correctly). It is still a real ordering hole for any future change whose two halves must land together. `release.yml`'s golden order DOES serialize W2 before W3 — **the race exists only on a direct push to `main`.**
  - **`supabase/migrations/00000000000000_baseline.sql` is NOT replayable from scratch, and nothing in CI notices.** Verified in `postgres:16-alpine` this pass: after shimming the Supabase `auth` schema it fails at the squashed migration-005 body — `ALTER TABLE public.trainings RENAME COLUMN runpod_job_id TO cloud_job_id` against a `CREATE TABLE` that already declares `cloud_job_id`, i.e. `ERROR: column "runpod_job_id" does not exist`. Harmless for production, which was built forward and never replays the baseline, and harmless for the `validate` job, which only pglast-PARSES each file (all 18 migrations and all 31 rollback files parse clean under `pglast==6.*`). **It would bite `supabase-migrate.yml::apply-branch`**, which builds an ephemeral Supabase Branch from the migration set — that job is currently skipped because the `SUPABASE_BRANCHING_ENABLED` repo variable is unset, so flipping that switch surfaces this defect first. Fix is to delete the two orphaned historical `RENAME`/mutation lines from the squashed baseline, not to re-squash.
  - **`supabase/rollback/README.md`'s ordered list is stale from 010 onward** (`CLAUDE.md` says "documents only through 009" — it is 009, and it has not been maintained since). 038's rollback twin was deliberately NOT slotted into that list; fixing the README is its own job.
  - **DO NOT "harmonise" `pi_agent/agent.py::origin_allowed` with `roboter_studio_control._origin_allowed` — it would break EVERY Pi POST.** The Pi predicate's own docstring says it is a "ported exact-host check" from the Windows twin, and the Windows twin has since grown `_ALLOWED_ORIGIN_SCHEMES` and `_ALLOWED_ORIGIN_PORTS` **and keeps `_ALLOWED_ORIGIN_HOSTS = {"localhost", "127.0.0.1"}`**. A Pi is reached at whatever LAN IP or mDNS name the school gave it, so its predicate deliberately allows the Pi's own literal hosts plus any IP literal that is one of this Pi's own addresses, and checks neither scheme nor port. Adopting the Windows host set would 403 every mutating `/api/system/` request from the wizard — arm scan, camera roles, HF token, „Umgebung starten", Update, Reset — on every Pi in the field. The divergence IS the design, exactly as it is for the two nginx Origin rules (fixed allowlist on Windows, host-relative `$http_host` on the Pi).
  - **The offline escape is not persisted, by decision.** `offlineOverride` is plain React state and dies with the document, so a reload re-shows the login gate for a student who took the escape. A `sessionStorage` latch would survive it, but that is a NEW persistence key and `sessionScope.test.js`'s coverage scan fails on any unregistered `edubotics*` key, so it forces an `IGNORED_STORAGE_KEYS` entry plus a written reason — more machinery than the residual is worth. Offline, `/version.json` is served by the local nginx out of the already-running image and cannot advertise a new buildId, so the `useVersionCheck` auto-reload essentially cannot fire; the two other reload paths are student-initiated; and the cost when it happens is one more ~10 s login attempt to re-obtain the offline proof, **not lost work** (the Roboter-Studio autosave is in IndexedDB, which nothing scrubs). Same shape: signed in > 1 h, network drops, then a reload → `getSession()` cannot refresh, returns `session: null`, and the gate asks again; the escape recovers it. **If a rig test shows either biting, the fix is the sessionStorage latch plus its registry entry — never a weaker gate.**
  - **No browser-side login timeout was built, deliberately.** Only the error-class predicate ships. The measured 10.5 s wait against an unreachable host is Node/undici's connect timeout; a browser's is OS-dependent and could be longer. **Decision rule:** if a rig test shows the browser hanging past ~20 s before rejecting, add a `setTimeout(…, 15000)` that sets the same `cloudUnreachable` flag — one `useRef` and one timer, **no new predicate**. Do not build it speculatively.
  - **A malformed (non-empty, scheme-less) `REACT_APP_SUPABASE_URL` still white-screens the app.** `createClient` throws at module evaluation and both Dockerfiles check non-emptiness only. Pre-existing and unrelated to the gate, which covers the EMPTY case through `lib/supabaseClient.js`'s `buildStub()` proxy (whose synchronous throw is what the gate's `try/catch` and the escape's no-`status` branch handle). Noted, not fixed.
  - **`docs/plans/student-session-lifecycle.md:329-333` is EXPLICITLY OVERRIDDEN by the owner.** That plan rejected login-gating the Daten tab because "a login gate would break the deliberately-supported never-signed-in recording path while leaving the underlying hole wide open on the wire." Both halves still hold as *statements*, and the gate ships anyway: the never-signed-in path survives through the attempt-gated offline escape, and the wire-level hole is untouched and stays on the books above. **The gate is not a substitute for ownership enforcement on the wire and must never be recorded as one.** Related, and now buildable: that plan's `:205-208` shelved an unuploaded-dataset warning because "the population that most needs it is precisely the students who never log in" — that population is now the offline escape alone. Out of scope this pass; worth scheduling.
  - **`gui/app/constants.py`'s `PORT_VIDEO_SERVER` / `PORT_ROSBRIDGE` carry no comment at all**, while the Pi twin's now explains that both are loopback-pinned since `4d1b046`. Neither pair has any non-test consumer; both were KEPT rather than deleted because the block they sit in is a deliberately complete port map (`PORT_CAMERA_INGEST` right below says so in its own words). Nothing in the Windows block is *false*, so it was left alone — but a reader who meets the corrected Pi block and then this one will notice the asymmetry.
- **[CLOSED 2026-08-07] Orange-Pi parity gap left by the P0 pass (2026-08-06, OWNER CALL).** All three Windows protections are now on the Pi: an Origin gate on `location = /rosbridge`, `/video/` narrowed from a PREFIX to an exact `= /video/stream` (which is what removes upstream's HTML-reflecting `stream_viewer` from the Pi's own origin — the escalation the gate would otherwise trust), and `proxy_hide_header Access-Control-Allow-Origin`. **The POLICY differs and must**: the student config's fixed `localhost`/`127.0.0.1` allowlist would lock out every Pi in the field, so the Pi rule is HOST-RELATIVE — the Origin must name the same AUTHORITY the request was addressed to, `if ($http_origin = "http(s)://$http_host")`. Driven with real traffic on nginx 1.27.5-alpine (the base `Dockerfile.opi` ships), the template rendered by nginx:alpine's own envsubst with the shipped `NGINX_ENVSUBST_FILTER`, and a fake upstream aliased `physical_ai_server` on a user-defined network so `resolver 127.0.0.11` resolves; ten source mutations are RED in `tests/test_rosbridge_origin_gate.py` and six were re-driven on the real nginx. **Two measured facts worth keeping, because the obvious reasoning about them is WRONG:** on a Pi served on `:80`, `$host` and `$http_host` AGREE — including on refusing `Origin: http://<pi>:8080` — so „`$host` would trust the :8080 XSS origin" is false. The real `$host` defects are (a) with `Host: pi.local:8080` it ACCEPTS a page on `http://pi.local` (a different origin, measured 200) while REFUSING the legitimate `http://pi.local:8080` (403), and (b) `$host` is taken from an ABSOLUTE-FORM request line, so `GET http://evil.com/rosbridge` with `Host: pi.local` + `Origin: http://evil.com` is ACCEPTED under `$host` and refused under `$http_host`. **Known, deliberate and fail-CLOSED:** `Host: PI.LOCAL` with a lowercase Origin is refused, because `$http_host` preserves case — no browser can produce that pair (the URL Standard lowercases the host during parsing) and the failure is a loud 403. Separately: the opi compose used to publish `:9090`/`:8080` on `${EDUBOTICS_BIND_HOST}`, which `constants.DEFAULT_LAN_OPEN="1"` makes `0.0.0.0`, and this entry recorded that as a settled decision on the grounds that "unpublishing hardens nothing". **That was REFUTED BY EXECUTION on 2026-08-08 and `4d1b046` pinned both publishes to `127.0.0.1:`** — `rosbridge_server` 2.7.0's `check_origin` is literally `return True`, so while the publish was LAN-bound the manager's `/rosbridge` Origin gate was *skippable* rather than defeatable: a handshake carrying any Origin straight at the published `:9090` returned 101 and could publish `/leader/joint_trajectory`. Debug now goes through `ssh -L 9090:127.0.0.1:9090`; the un-tunnelled LAN fallback is deliberately gone. Fenced by `tests/test_rosbridge_origin_gate.py::OpiDebugPublishesAreLoopbackPinned`, which replaced a test that asserted merely that the ports appear and therefore pinned the hole open. **THE ACCEPTED RESIDUAL THEREFORE MOVED, and this entry's old wording pointed at a port that is no longer LAN-reachable: it now lives on `:80`**, which is LAN-open by necessity because it is how a school reaches the wizard at all. A non-browser client on the LAN — `curl`, `wscat`, roslibpy — can send whatever Origin it likes to `/rosbridge` there and drive the arm, and an Origin gate provably cannot stop it: measured, even with the empty-Origin allowance removed, `curl -H 'Host: <pi-ip>' -H 'Origin: http://<pi-ip>' …/rosbridge` is still proxied, because Origin is a BROWSER control set by the user agent and anything that can forge a header can forge that one. **The answer is the dedicated-VLAN guide, not a tighter regex.** The manager's own `:80` still follows `${EDUBOTICS_BIND_HOST}` and must keep doing so: `LAN_OPEN=0` is kiosk mode, never a hardening knob.
  **Never remove the empty-Origin allowance from the WINDOWS `nginx.conf`, either.** If `''` is dropped from the `$edubotics_origin_ok` map, nginx answers **403** to `check_rosbridge`'s probe — and 403 is not in `health_checker._UPSTREAM_DOWN_STATUSES = {502, 503, 504}` (verified 2026-08-31), so `_upstream_alive` and therefore `check_rosbridge` would return **True forever**: precisely the "health check that could never fail" that module's own header exists to prevent. The gate would still look fine and the liveness signal would be silently dead. The allowance is load-bearing on its own terms too — a browser CANNOT omit Origin on a WebSocket handshake (the UA sets it, not script), so a blank Origin provably did not come from the attack the gate exists to stop, while a `file://` page sends the literal `null`, which is non-empty and refused (verified against nginx 1.27.5).
- **[FIXED 2026-08-06 — P0-3, see the P0-pass bullet]** **STUDENT HANDOVER IS BROKEN IN BOTH DIRECTIONS (2026-08-06 audit, two independent agents).** Scenario: one shared Windows account, student A closes the window and walks away without clicking „Abmelden" — the path students actually take. **Two ACT-AS leaks, at two layers, neither needing an attacker.** **(1) A's live Supabase session survives** into B's launch: `lib/supabaseClient.js` calls `createClient` with **no options**, so auth-js defaults `persistSession` AND **`autoRefreshToken`** — a durable, self-renewing credential, not an expiring JWT. It authorises `routes/me.py::export_my_data` (A's profile, trainings, memberships, workflows, datasets **and teacher-written progress entries** in one call — the GDPR Art.-15 subject-access endpoint is the breach vector), `POST /me/delete`, and `/trainings/start` against A's shared workgroup credits. It also restores **A's Blockly program and Backpack into B's editor**, because `useAutosave`'s bucket is namespaced by the Supabase uid that itself leaked — which makes `sessionScope.IGNORED_STORAGE_KEYS['edubotics:workshop:autosave']`'s "already per-student" rationale **false on this path**. **(2) `data_manager.DataManager.__init__` builds `_save_repo_name` from the CLIENT-SUPPLIED `task_info.user_id`**, not from the token, while `InfoPanel` auto-selects `hfUserList[0]` (= `whoami` on the rig token). So B's recordings upload **into A's HF namespace and SUCCEED**. **Root cause is one fact:** `clearStudentScopedStorage` is reachable ONLY through `signOutStudent`, whose five call sites are all explicit clicks or a 401/403 bounce — no unload handler, no boot path, no Python path. The storage registry itself is **complete** (37 Web Storage sites, one `idb-keyval` importer, zero raw IndexedDB / cookies / Cache API / service workers / OPFS), so registry work cannot fix this. **The recommended fix is a spawn-time wipe, not a close-time one:** `webview_window.destroy_all` uses `Popen.terminate()`, a hard kill that never delivers WinForms' `on_close`, and a close can anyway be a crash or a power-cut — so no close hook can be made reliable, while spawn always runs. Wipe in `open_student_window` right after the live-child short-circuit, against an explicit `storage_path` under `%LOCALAPPDATA%\EduBotics\` (which also fixes the profile currently living in the **roaming** `%APPDATA%\pywebview`, so today A's session follows to another PC). Keep `private_mode=False`. `webview_window.py`'s module-level imports are stdlib-only, so a deps-free fence joins `ci.yml::python-tests` with no new job; it must AST-assert that the wipe target and the `storage_path=` argument resolve to the **same** constant — drift there is silent. **Residual after that fix**, stated plainly: the refresh token is deleted locally, not revoked at Supabase; autosave crash-recovery across *process restarts* is lost (Backpack included); and a handover on a **still-open window** is untouched by any of this.
- **[2026-09-01 ADDENDUM] That close is now CONFIRMATION-GATED on the arm-scan path.** `gui_app.py::_scan_arms` asks „Beim Scannen wird das EduBotics-Fenster geschlossen und du wirst abgemeldet. Nicht gespeicherte Änderungen gehen verloren. / Trotzdem scannen?" (`default=messagebox.NO`, matching `_prompt_finalize_install`'s data-loss dialog — a stray Enter must not end the lesson) **only when `webview_window.has_live_window()`**. Confirm ⇒ the scan proceeds byte for byte as before. Decline ⇒ nothing at all: no teardown, no `destroy_all`, no `ensure_environment_stopped`, no status text, no log line, no progress bar, `_scanning` untouched, the button left enabled. `has_live_window()` is **PROCESS-LOCAL for the same reason `destroy_all` is** — `_process` is written only by this process's `open_student_window` — so a window orphaned by a crashed GUI reads as ABSENT, no dialog appears, and the scan proceeds exactly as it did before the dialog existed. That is the harmless direction and needs no fix beyond the kill-by-cmdline mechanism the next entry asks for. Two deliberate non-decisions: the answer is a SNAPSHOT and is not re-taken (if the student closes the window while the dialog is up, confirming runs a scan whose `destroy_all` is a harmless no-op and declining does nothing — both already correct), and `has_live_window()` is called with **no `try/except`** (it is a lock plus `Popen.poll()`; fail-open would restore the silent logout this exists to remove, fail-closed would make the scan button unusable). `_scan_cameras` still shares `self._scanning` with `_scan_arms`, so a `root.after`-scheduled camera scan can start while the arm dialog is up — pre-existing behaviour for every modal in this file, left alone; the new `_scan_confirm_open` flag guards only re-entry into `_scan_arms`.
- **[NOT FIXED, deliberate] A WebView2 child ORPHANED by a crashed or Task-Manager-killed GUI holds the previous student's session and nothing in the product reaches it.** `webview_window.destroy_all()` and `has_live_window()` both read the module global `_process`, written only by *this* process's `open_student_window` — so every one of the four teardown call sites (`_on_close`, `_stop_environment`, `_run_prerequisite_checks_body`, `_scan_arms._do_scan`) is blind to a child this GUI did not spawn. That matters most on the path whose own status text is „Vorherige Sitzung wird aufgeräumt…": read literally the name promises cleanup of a *previous GUI process*'s session, and `destroy_all()` there is a **no-op for exactly that case** (it was added for the four same-process re-check paths, where it is real). Closing an orphan needs a new kill-by-cmdline-sentinel sweep at GUI startup — find any `EduBotics.exe --webview` not owned by us and close it by PID — which is a new mechanism with a new failure mode: it can close the window of a legitimately running second instance, i.e. exactly the redesign the stated threat model forbids. **Recorded, not built.** Mitigation that already exists: an orphan is harmless for the Blockly autosave, because `useAutosave` namespaces every bucket by supabase uid or by a **sessionStorage** browser-session id, and sessionStorage dies with the window. The exposure is the Supabase session in the orphaned window itself.
- **[FIXED 2026-08-07] EVERY browser-side teardown hook is dead, because the GUI hard-kills its child (2026-08-06).** `destroy_all` now posts `WM_CLOSE` to every top-level window owned by the CHILD pid (`EnumWindows` + `GetWindowThreadProcessId`), waits up to `GRACEFUL_CLOSE_TIMEOUT_S` (2.5 s, ending the instant the child exits) and only then terminates. `_on_close` derives „is a stack up" from `docker_manager.any_container_running()` — ONE bounded `docker ps`, ANY not ALL, failing closed so a broken probe can never make the window unclosable — instead of from `self.running`, and the four process-local teardowns (camera bridge, phone receiver, `:8769` bridge, webview child) now run UNCONDITIONALLY. **STILL UNPROVEN and it needs a dev rig:** that a `WM_CLOSE` really produces a DOM `pagehide` inside WebView2. The chain (WM_CLOSE → WinForms `FormClosing`/`FormClosed` → pywebview `closing`/`closed` → WebView2 teardown → DOM `pagehide`) is reasoned from the APIs, not observed — there is no Windows, no WinForms and no WebView2 on the dev host. Check it by claiming a Jetson, closing EduBotics, and confirming the lock releases well inside the 5-minute sweeper window. **`_focus_existing_window`'s `FindWindowW(None, "EduBotics")` ambiguity is UNCHANGED and pre-existing** — the fix routed around it (PID matching) rather than fixing it. Original diagnosis, kept for the reasoning: `webview_window.destroy_all` uses `Popen.terminate()`; on Windows that is `TerminateProcess`, which delivers neither WinForms' `FormClosed` nor the DOM's `pagehide`/`beforeunload`. Three shipped consequences, one with a direct classroom symptom: **(1) the Jetson release beacon never fires** — `useJetsonConnection` releases the exclusive claim on `pagehide`, so the lock leaks for the full 5-minute sweeper window and **the NEXT student is refused the Jetson**; (2) `JogPanel`'s unmount re-torque never runs, so a hand-guide session can end with the follower left limp until the 30 s `_manual_idle_watchdog` catches it; (3) `RecordPanel`'s teardown never runs. All three are fixed by a graceful close (`WM_CLOSE`/`events.closing`) before the terminate — but **any such close MUST match by PID**: `gui_app.py`'s tkinter root and the pywebview child are both titled exactly `"EduBotics"`, so `_focus_existing_window`'s `FindWindowW` is **already ambiguous today**, and a close-by-title would risk closing the setup GUI instead of the browser. Related: **`gui_app.self.running` is not a reliable "containers are up" signal** — `_do_start`'s `finally` clears it on any failure that occurs AFTER `start_containers` succeeded, so `_on_close`'s `running == False` branch can leave a live stack, camera bridge and phone server behind (that branch already omits `_stop_camera_bridge()`, see the handover bullet).
- **Recorded datasets live INSIDE `huggingface_cache`, not `ai_workspace` — the volumes cannot be scoped per student (2026-08-06).** `PhysicalAIServer.DEFAULT_SAVE_ROOT_PATH` is a hardcoded `Path.home() / '.cache/huggingface/lerobot'` with **no env override anywhere**, and compose mounts `huggingface_cache:/root/.cache/huggingface` — so student recordings sit in the same volume as the shared model cache AND the `huggingface-cli login` token files. **Not separable at the volume level; fully separable at the file level.** This is why `factory_reset` cannot be made correct by re-pointing it: any per-student cleanup has to walk paths, not volumes. Two corollaries: a student's own trained policy is *already* re-fetched on every container recreate (it lands in the writable layer, not the volume), so "preserve the cache" only ever meant the shared backbone; and **`<dataset>.session.json` answers the wrong question** — `data_manager._finalize_dataset` clears it BEFORE `push_to_hub` is evaluated, so it means "finalize succeeded", never "uploaded". Anything gating on "is this work safe to delete" needs a new `<dataset>.uploaded.json` sibling written by `upload_huggingface_repo`. **And `HF_TOKEN` must NOT be promoted to a `config_generator.MANAGED_KEYS` entry** to make it clearable: `gui_app._rs_set_leader_mode` regenerates the `.env` at RUNTIME on the `:8769` control-server thread, so a managed token would be stripped mid-session by the Roboter-Studio leader toggle.
- **Orange Pi: the agent's only identity control is unreachable from its own UI (2026-08-06).** `pi_agent/agent.py::handle_hf_token` implements empty-input → clear and answers „Token entfernt.", but `SystemPage.js` **disables the submit button on an empty field**, so a Pi's HF token can be set and never cleared. One-line frontend fix. **Do NOT twin the Windows session-lifecycle model onto the Pi**: the Pi's browser is the student's OWN device (hence `rsControlBase`/`localRosbridgeUrl`, the port-80 consolidation and `setup.sh`'s QR code — the Pi installs no browser), so no shared browser profile exists to leak, and the Pi is CONCURRENTLY multi-tenant, where a wipe at „Umgebung starten" would destroy other students' live work. `pi_agent/config_generator.py` correctly lacks the whole token-binding layer and must stay that way.
- **[MOSTLY FIXED 2026-08-06 — P0-1 + P0-2. The roster itself is KEPT by owner decision; `set_hf_user_callback`'s token-on-the-wire violation of the „no token UI" invariant is NOT fixed and is folded into the org-namespace spec.]** **The unauthenticated rosbridge surface is a cross-student roster, not a latent attack — it is the shipped „Daten" tab (2026-08-06).** `PhysicalAIServer::get_user_list_callback` is a bare `os.listdir` returning every HF username that ever recorded on the rig, `get_dataset_list_callback` accepts **any** `user_id`, and the Daten tab is login-gated in the BROWSER only, since the 2026-08-31 student login gate (`utils/authGate.js`; before that it had **no auth gate** at all and only Training/Inferenz did) — the roster is still reachable by every student who logs in, and by any client at all on the wire, so **nothing about this entry's severity changes and the login gate must never be recorded as closing it**. From the same dropdown, `edit_worker.run_edit` uses `delete_dataset_path` verbatim with **no root confinement and no ownership check**, so any student can destroy any other's episodes. Also on the wire: `control_hf_server_callback` forwards arbitrary `mode`/`repo_id`/`local_dir`/`author` to the HF worker; `set_hf_user_callback` → `data_manager.register_huggingface_token` runs `huggingface-cli login`, landing a token **inside the `huggingface_cache` volume** where `config_generator.bind_hf_token_to_this_machine` structurally cannot see it (dead in React, live on the wire); and `communicator.browse_file_callback` has zero path confinement and explicitly un-hides `.cache`. **Unconfirmed but architecturally implied and worth a 5-minute rig check FIRST:** React is served on `:80` and connects to `ws://host:9090`, a different origin — if rosbridge therefore accepts arbitrary origins, any website a student visits can drive the arm and read datasets, and the loopback bind is not a boundary on a shared PC anyway.
- **[FIXED 2026-08-06 — P0-4 (both layers, PROVEN in a real `ros:jazzy` colcon build: `TaskInfo().private_mode == True` with `push_to_hub == False` as the control, and `rosidl_adapter` parsing it as a FIELD default rather than a constant). The erasure half is PATCHED, not fixed — see the P0-pass bullet and `docs/plans/2026-08-06-hf-org-namespaces.md`.]** **`_upload_dataset` contradicts its own documented fail-safe, on a path carrying video of children (2026-08-06).** `data_manager._upload_dataset(self, tags, private=False)` — while its own docstring says *"It defaults to True so a missing/garbled flag fails safe to private — classroom recordings can contain children's faces / audio."* React always sends `private_mode: true`, so it is latent; **any rosbridge client omitting the flag gets a PUBLIC repo.** Related and worse: **misattributed recordings cannot be erased.** `teacher.py::_delete_student_hf_artifacts` enumerates from the `trainings` table only (a recording never used for training is never found) and authenticates with the **platform** token against **student** namespaces, where the 403 is swallowed; `me.py::delete_my_account` deletes nothing and nothing drains its queue. **The standing owner decision to accept the machine-bound HF token was justified with the wrong rule** — `CLAUDE.md`'s invariant says "no token UI in the browser", which is correct and which `set_hf_user_callback` violates today; it does not say "one token per machine". The audit's recommendation is **one school/org HF account with per-student sub-namespaces** (`<org>/<supabase_uid>-<task>`), the only option that fixes attribution and erasure together and stops 14-year-olds needing personal accounts on a US platform; it touches `_save_repo_name`, migration 026's `register_dataset_safe` HF-author anchor and `datasets.py::sync_datasets`, so it needs a `docs/plans/` spec. **Per-Windows-account was assessed and REJECTED**: images live inside the WSL distro, so 30 accounts ≈ 30 × (rootfs + 5.56 GB + datasets), and it would not fix the misattribution anyway (a client-supplied-string bug, not an isolation bug). Two smaller GUI defects found alongside: `docker_manager.factory_reset` never touches the `.env`, so the one cleanup tool a teacher has wipes all three volumes — **including the machine-owned calibration** — and leaves the token; and `gui_app._on_close`'s `running == False` branch neither stops containers nor calls `_stop_camera_bridge()` (both present in the running branch).
- **Two SHIPPED Roboter-Studio grasp defects, found by end-to-end simulation (2026-08-06). [BOTH RESOLVED 2026-09-09 — kept for the reasoning, and because the SECOND one was resolved by being judged CORRECT, not by being changed.]** **(1) is FIXED**: `ArmProfile.grasp_held_max_rad` (0.12 on edu6, 0.10 on edu1) replaces the OMX-band −0.35 fallback, and `GRASP_HELD_MIN_TRAVEL_FRAC` adds the second half of the HELD test so a close that never executed stops reading as a grasp. The audit that verified it **partly refuted the claim below**: `main` already answered correctly for every close commanded *through a block*, so the real window was narrower than „CONSTANT TRUE" — a HELD query issued **before the first close of the run**. **(2) is NOT a defect** and the code is unchanged where it matters: a zero-trust audit measured the 47.9/12.7 figures as non-comparable (see the correction inside), found the capability gap on the shipped AprilTag path to be **zero**, and concluded that „standalone fails loud" is RIGHT for a cause the world can fix — the alternative lets „Greife" fail and „lege ab" then place nothing on a green run. What WAS wrong is that a cause the STUDENT can fix (a wrong value in a `Greifziel` socket) was swallowed by „Solange sichtbar"; `GraspSkip` is now classified by CAUSE, so a program error ends the run while a world problem stays recoverable. Both are current-code and independent of whether „Eigene Objekte" is ever built. **(1) `motion.check_grasp_held` is VACUOUS on edu6.** `motion._held_threshold_rad` falls back to the fixed global `GRASP_HELD_MAX_RAD` (−0.35) whenever `ctx.last_commanded_close_rad` is unset — and it is written ONLY by `_execute_pickup` / `close_gripper` / `close_on_object`, all ctx-scoped. edu6's gripper band is 0…1.75, so every readback exceeds −0.35: measured `{True}` across all 21 samples of the band. Two consequences. `GRASP_RETRY` fires on `held is False` ONLY, so **it is dead code on edu6** — while being cited as a live mitigation in `CLAUDE.md`, in `perception_blocks.grasp_object` and in the „Eigene Objekte" plans. And `held is None` claims the tag anyway, so **a failed grasp is reported as SUCCESS**: the program logs „fertig" over an object that never moved. Worth ~1 pp of end-to-end success; the silent-success half is what matters. The in-code note on `wait_until_held`/`grasp_held` (see the triaged-bugs bullet) documents the OPEN-gripper case but not this one — the threshold path itself is ctx-scoped, which is the wider bug. **(2) `motion.GraspSkip` subclasses `WorkflowError`**, so a per-instance skip that `grasp_object` handles cleanly inside „Solange sichtbar" **aborts the entire program** when the student wired standalone „Greife" blocks instead. **[CORRECTED 2026-09-08 — the two figures this bullet used to quote were not comparable, and the advice they supported was backwards.]** It formerly read *"Measured over a 3-object program: loop form completes 47.9 % of runs, block form 12.7 %. Any tutorial or generated starter program should therefore emit the LOOP form."* All three parts are wrong. **(i)** The two numbers are DIFFERENT METRICS out of `docs/plans/eigene-objekte-e2e/mc2.py` (gitignored, untracked, `n_sessions=120_000`): 12.7 % is its `program_all3_moved`, 47.9 % its `program_reports_success`. `roboter-studio-eigene-objekte-cv.md` §3.1 labels them correctly and footnotes the loop column *"the loop reports green over unmoved objects"*; this bullet took one column from each, called both "completes", and dropped the footnote. Like-for-like all-moved is **19.4 % vs 12.7 %** — under 7 points, not 35 (re-run 2026-09-08: 19.5 / 12.8; the 47.9 itself does not reproduce, that harness now yields 55.6). **(ii)** `mc2.py` models the UNBUILT „Eigene Objekte" recogniser — its dominant terms are `auroc`, `sigma_c`, `grouped_acc`, `frr`, `n_distract`, and at least eight of its inputs are commented in the file as unmeasured or synthetic. The shipped path is AprilTag-only, where identity is exact. Calling that output "Measured" was wrong. **(iii)** On the SHIPPED code the capability gap is ZERO: 60 random scenes per cell against the real `Interpreter`, real `IKSolver` and real catalog, **the two forms move identical cubes** (2.22 of 3 in the realistic cell) while the loop reports 100 % green against the block form's 33.3 %. The spread is entirely REPORTING, and it runs the wrong way — so the old advice recommended the form that HIDES the failure. There is also no consumer for it: `physical_ai_manager/public/tutorials/` holds two tutorials, neither uses `edubotics_grasp_object` or `edubotics_while_visible`, and no starter-program generator exists. **The real defect underneath is narrower and is being fixed:** `GraspSkip` conflates a cause the WORLD can fix (nothing visible, out of reach, unreadable tag) with a value the STUDENT wired wrongly, and a loop swallows the latter — measured, a wrong variable in „schließe um" makes the arm descend three times with an open gripper and the run report GREEN.
- **Latent: `object_catalog.build_object_catalog_response` calls `fixed_catalog()` with NO `profile_id`**, so `GetObjectCatalog` serves the OMX catalog to an edu6 rig. Benign ONLY because `gripper_close_rad` — the single field that differs between `_FIXED_CATALOG` and `_FIXED_CATALOG_EDU6` — is not among the six response arrays built by `catalog_response_fields`. **Adding any per-profile field to that response makes it a live wrong-value bug.** (`_load_object_catalog` on the node does pass the profile id; this is the srv path only.)
- **NEW RIG GATE (2026-09-08) — the AprilTag may be too small for the shipped lens, BY CONSTRUCTION. Measure the scene camera's real FOV and working height before trusting any detection-reliability claim.** Found while auditing the „Solange sichtbar" reclaim; flagged, NOT concluded, because it rests on a figure taken from this tree's own comments rather than from a measurement of the shipped camera. **The detector is a CLIFF, not a flicker** — measured with the shipped `workflow.perception.Perception` (tag36h11, `quad_decimate=1.0`) on rendered 640×480 JPEG-q80 frames, apparent tag size goes **100 % at 15 px → 50 % at 14 px → 0 % at 13 px**, and at ≥16 px it survives every other degradation short of its own cliff (glare, defocus ≤5 px, contrast 0.08, JPEG q20, noise σ50, 70° viewing angle, 13 % occlusion all detect 100 %). **The arithmetic:** `CLAUDE.md` and `calibration_manager` both describe the Innomaker scene cam as a **~102° lens**, and `CAMERA_WIDTH` defaults to 640, giving `fx = 259 px`. The shipped catalog tag is **24 mm** (`EDUBOTICS_TAG_SIZE_M`, and `tools/generate_apriltags.py` prints the sheet at exactly that). So a tag measures **15.5 px at 0.40 m, 12.4 px at 0.50 m, 11.3 px at 0.55 m** — i.e. at any plausible working height it sits AT or BELOW the cliff. A 70° lens would give 21.9 px at 0.50 m and be comfortably safe. **If the 102° figure is right, named-object detection is marginal by construction and the fix is a bigger tag or a narrower lens — not a software change**, and every "occasional miss" anyone has attributed to lighting or luck is this instead. **What the rig must answer:** the scene camera's actual horizontal FOV, its actual height above the table, and the resulting apparent tag size in pixels at the near and far edges of the pick band. If the answer lands under ~16 px, raise `EDUBOTICS_TAG_SIZE_M` and reprint the sheet (both are one variable and one command) before tuning anything else.

- **R4 addendum — jaw friction is unmeasured and no constant for it exists anywhere in the repo.** The edu6 fingers ship as bare printed `Claw_left_finger.STL` / `Claw_right_finger.STL` with no pad, no friction coefficient and no material spec; `collision_detector`'s "servos + friction are identical across classroom rigs" comment is about SERVO friction, not jaw-to-object. Grasp-synthesis simulation is one-sided in this parameter: µ 0.30 → 0.15 costs 5.8 pp of grasp success and nearly doubles rotate-out failures, while µ ≥ 0.5 saturates. **So R4 should measure µ, or the rig should simply get a rubber pad** — hours of work, and it moves the arm into the saturated regime where the number stops mattering. Until then any published grasp-success figure carries an unquantified µ assumption.
- **Branches: audited clean 2026-07-26.** No local branches besides `main` exist any more — the
  previously-listed `roboter-studio-modernization` and “~10 local branches of already-shipped work”
  are GONE, so that pruning item is closed. No stashes, one worktree. On the REMOTE, 20+
  `claude/*` + `feat/*` + `fix/*` branches report commits “not in main”, but that is the standard
  squash-merge false positive (their CONTENT is in main; their commits are not ancestors).
  All 20 were scanned for edu6/driver/solver files; **exactly two hit**.
  `origin/feat/lerobot-v0.5.1-dataset-v3` is the WIP of the SHIPPED LeRobot 0.5.1 upgrade, 702 of
  its 717 files being vendored upstream LeRobot (which main never carries, Rule §5) and it still
  edits the retired `overlays/training_manager.py` — stale. And verified in detail,
  `origin/claude/6dof-joint6-180-degrees-623iwv`: its `_EDU6_JOINT_LIMITS_RAD` is BYTE-IDENTICAL
  to main's full ±180° (despite a commit message claiming a ±178° trim), `roll_from_joints` exists
  in main for both solvers, and main is a functional SUPERSET (the branch lacks 2,602 lines main
  has, incl. `tools/generate_edu6_mat.py`). **Nothing to salvage; do not merge it.** Pruning the
  remote branches is a `push --delete`, i.e. ask first.
- **Phone-camera non-goals still open**: recording + React-grid listing of the phone stream require `camera_topic_list` (a static per-robot `config/*.yaml` list) to become env-overridable and the `len(camera_topic_list)` recorder/inference gates reworked — don't attempt casually.
- **Reviewed and deliberately NOT fixed (2026-08-02, Orange-Pi parity round).** Each was measured, judged low-harm, and left alone; recorded here because a note in a gitignored handoff directory is not a record.
  - **A Windows fast-rehydrate can replace a fresh in-session scan across arm families.** `gui_app._try_rehydrate_arms` reads the profile out of the **`.env`** (a previous session's) and both of its "never overwrite a scan" guards then ask `_hardware_ready(that .env profile)`. After a cross-family switch, a fresh scan can never satisfy that profile — which is exactly what the 2026-07-29 family check guarantees — so the guard is structurally unable to fire in the one case it is needed, and the rehydrate silently restores the OLD family's ports over the new scan. It needs no race: `_try_rehydrate_arms` is the last statement of the prerequisite-check body, which runs on a worker for minutes while „Arme scannen" stays enabled. **Never unsafe** — `_update_start_button` and `_start_environment` both re-gate on the SELECTED profile, so the arm cannot start; the only cost is a student told „Letzte Arm-Konfiguration wiederhergestellt" with no hint that the scan was discarded. Recoverable by rescanning. The fix would be to key both guards on the selected profile, not the `.env` one.
  - **`ARM_FAMILY_MISMATCH_DE` is plural („Die gescannten Arme") on rigs that have exactly one arm.** A follower-only profile judges `[follower]` only, so the sentence reaches a single-arm rig — beside a Pi card that `b3c1109` deliberately made singular („Arm scannen" / „Arm erkannt" / „Roboterarm"). That commit scoped the singular convention to the **Pi** and explicitly carved Windows out (the GUI stays plural everywhere), so only the Pi copy contradicts it. Fixing it is not a one-line edit: `tests/test_gui_robot_type.py::test_the_two_platforms_say_the_same_thing` PINS the two platforms' strings equal, so either the Pi string diverges and that pin changes, or both go profile-aware.
  - **Three guards name a behaviour without fencing it** (each MEASURED by mutation, 2026-08-02):
    - `gui_app._arms_conflict_with_family`'s **leader leg** — deleting `arms.append(self.hardware.leader)` outright leaves all 808 robotis_ai_setup tests green. Only its `if scan_requires_leader:` CONDITION is fenced (making the append unconditional does fail). The **Pi twin IS fenced** (`test_lifecycle.py::TestCrossFamilyInvalidatesTheScan::test_the_eager_write_judges_the_LEADER_it_emits_too` kills the same mutation), so this is a one-sided hole: add the mirror case (`omx_full` + wrong-family LEADER + right-family follower) to `HardwareReadyArmFamilyTest`.
    - The **`typeof` narrowing** on `hardware_ready` in `SystemPage.js` — both `agentStatus?.hardware_ready ?? armsBoth` and `|| armsBoth` survive, because every fixture in the tree is `true`/`false`/absent. The FALLBACK is fenced; the type check is not. Needs a fixture with a non-boolean `hardware_ready` (`'false'`, `0`, `{}`) plus `both: true`.
    - **`test_arm_scan_twin_lockstep._signature` cannot see `**kwargs`** (nor a trailing `*args`): it reads `posonlyargs`/`args`/`kwonlyargs`/defaults and never `node.args.vararg`/`kwarg`. Adding `**kwargs` to the Windows twin alone stays green — the exact "one twin silently accepts a keyword the other raises `TypeError` on" class the file exists to close.
  - **`MANAGED_KEYS` has no cross-platform lockstep, and the two sets have already diverged.** `gui/app/config_generator.py::MANAGED_KEYS` (9 keys) vs `pi_agent/config_generator.py::MANAGED_KEYS` (13); 8 are shared, `EDUBOTICS_CAMERA_NAMES` is Windows-only, and `EDUBOTICS_CAMERA_SOURCE`/`EDUBOTICS_LAN_OPEN`/`EDUBOTICS_BIND_HOST`/`EDUBOTICS_ROS_NET_SUBNET`/`EDUBOTICS_ROS_NET_GATEWAY` are Pi-only. Every divergence READS as deliberate (the Pi is always `usb_cam`; the LAN/subnet keys have no Windows analogue) but nothing asserts it: the only test naming `MANAGED_KEYS` anywhere is `pi_agent/tests/test_config_generator.py::test_managed_keys_lockstep_all_emitted`, which is the Pi's INTERNAL set↔emitter check (every managed key is actually emitted by both generators), not a cross-platform one. **Scope this precisely — the sibling claim is false for `generate_env_file`**, which IS cross-platform tested twice in that same file: once pinning the two twins' German refusal message byte-equal, once pinning the deliberate `EDUBOTICS_FOLLOWER_ONLY=0`-vs-omitted divergence. So the gap is specifically the KEY SET, and what it admits is a managed key added on one platform whose stale hand-pinned value is then never SUPERSEDED on the other — the `manifest unknown` scar class, one platform at a time.
  - **`docs/ORANGE_PI_IT_NETZWERK.md` still instructs IT to open 8080/9090, in SEVEN places** (`:19`, `:36`, `:75`, `:76`, `:166`, `:167`, `:172`) — deliberately out of scope, recorded so it is not lost. Only TWO of the seven genuinely contradict the port-80 consolidation: the troubleshooting rows `:166`/`:167` route a teacher to „Portfreigabe Schritt 3" for symptoms that in Pi mode cannot have that cause, directly against `docs/ORANGE_PI_TEACHER_RUNBOOK.md:213/215` („9090 und 8080 muss die IT **nicht** freigeben" / „**Nicht** die Ports"), and `:166` additionally quotes „Verbindung zu Port 9090 blockiert", which is not a shipped string — the real one is `piMode.js::PI_PORT_BLOCKED_HINT`. The other five merely ask for more than the browser needs, which stays consistent with the compose host-publishes of `:9090`/`:8080` remaining DEBUG/ROLLBACK only. `23db06a` fixed the RUNBOOK and named `:107`/`:163`/`:166` as deliberate residue; **`:167` was not in that list**, so it is an unlisted member of the same defect class. The file has never been revised since the consolidation.
- **From the shared-Windows-account round (2026-08-02).** One German school runs many student PCs under ONE shared Windows admin account, in parallel. That round machine-bound `ROS_DOMAIN_ID`, made a wrong-account install diagnose itself, and stopped student identity bleeding through the shared browser profile. What it left behind:
  - **The `.env` roaming sibling — the TOKEN half is FIXED (2026-08-03), the rest is bounded.** The very directory that motivated the `ROS_DOMAIN_ID` work, `%LOCALAPPDATA%\EduBotics\`, also holds `.env`, which carries **`HF_TOKEN`** — deliberately unmanaged precisely so it survives every regenerate and Factory Reset, which is exactly why a roaming profile / FSLogix container / AppData redirection / golden image carried one student's HuggingFace credential to every PC in the school. `HF_TOKEN` is now stamped with `HF_TOKEN_MACHINE = sha256(_read_machine_id())` and a token whose stamp does not match is DELETED, not merely ignored (mechanism, the two enforcement halves and the `EDUBOTICS_HF_TOKEN_ANY_MACHINE` opt-out: `CLAUDE.md`'s HuggingFace-token bullet). What remains, and why each is acceptable:
    - **The clone that is neither generalized nor renamed defeats it**, for the identical reason it defeats the domain derivation (see the entry two bullets down): both halves of `_read_machine_id`'s composite are identical on such an image, so the stamp MATCHES on the clone and the token is accepted. Self-limiting in the same way — such a fleet also has duplicate NetBIOS names — and `EDUBOTICS_MACHINE_ID` is the per-PC escape hatch for both.
    - **The ports still travel, and still fail safe**: `fast_rehydrate_arms` returns `(None, None)` on ANY mismatch and only PROMPTS a rescan, and `serial_path_family_conflict` refuses a cross-family port outright.
    - **`phone-cert/` still travels, unfixed and out of scope.** A copied self-signed cert + key is not the weak link on that surface: the phone receiver binds `0.0.0.0:8444` UNAUTHENTICATED by design (body-size gate only), so the cert authenticates nothing that the listener does not already give away.
  - **IT-facing recommendation, recorded here because there is no Windows sibling to `docs/ORANGE_PI_IT_NETZWERK.md`.** `%LOCALAPPDATA%\EduBotics` should be EXCLUDED from roaming profiles, FSLogix profile/ODFC containers and AppData folder redirection, and a golden Windows image should be captured BEFORE a first EduBotics launch (or with `%LOCALAPPDATA%\EduBotics` deleted and `%ProgramData%\EduBotics\wsl\ext4.vhdx` left to the installer). It holds three per-machine things — the arm/camera ports, `phone-cert/`, and the HuggingFace token — plus a multi-GB VHDX path assumption, none of which mean anything on another PC. **This is an optimisation, not a requirement**: the token is fingerprinted, the ports fail safe and `ROS_DOMAIN_ID` is derived per machine, so a roamed profile is now correct-but-noisy (one German Protokoll line and a re-entered token) rather than dangerous. A new top-level doc was not created for a single paragraph — the existing Pi doc is a German end-user/network ticket, this is a Windows domain-admin note, and a fifth top-level doc with one reader is less likely to be found than a paragraph beside the defect it mitigates. Fold it into a Windows IT doc if one is ever written.
  - **Every existing Windows install lands on a NEW `ROS_DOMAIN_ID` once**, on first launch after upgrade, silently and by design: the seed changed from `uuid.getnode()` to `_read_machine_id()` — and the value it lands on is the CANONICALISED one (`_canonical_computer_name`: short name, stripped, UPPER-cased, composed with `MachineGuid`), not the raw `ActiveComputerName` an earlier draft of this entry described. **The shift is exactly once per PC in the field, with no exceptions**: `git log --all -S"_read_machine_id"` / `-S"MachineGuid"` / `-S"_canonical_computer_name"` return NO commits, and `HEAD`'s `gui/app/config_generator.py` still carries the `uuid.getnode()` seed, so no released build ever shipped a pre-canonicalisation `_read_machine_id` and there is nothing for a second shift to happen to. A rig moves twice ONLY if it ran an intermediate working tree of this change set — i.e. a dev machine, not a school PC. Verified safe — `gui_app::_run_prerequisite_checks_body` calls `docker_manager.ensure_environment_stopped()` after the dockerd-ready gate and BEFORE every `.env` regenerate, so no container survives on the old domain. No log line was added because `gui_app` already echoes the whole generated `.env` line-by-line into the Protokoll, so `ROS_DOMAIN_ID=<n>` is support-visible on every „Umgebung starten". Operators keeping per-PC notes of the old value should know. Related residue: the old cache file `%LOCALAPPDATA%\EduBotics\.ros_domain_id` is now read by nothing and is not deleted, so a support engineer reading it will see a number the rig no longer uses.
  - **`"²".isdigit()` is True but `int("²")` raises.** `EDUBOTICS_ROS_DOMAIN="²"` (or any other Unicode digit-like character) passes the `.isdigit()` guard and then throws an unhandled `ValueError` out of `_resolve_ros_domain_id` on BOTH twins — on Windows straight out of `generate_env_file`, on the Pi additionally via a hand-tampered `.ros_domain_id`. PRE-EXISTING, not introduced by this round; the guard should be a `try: int(...)` or `str.isascii() and str.isdigit()`. It is an unhandled exception, not an out-of-range domain: no path RETURNS a value outside [0, 232].
  - **A clone that is neither generalized nor renamed still collides.** `_read_machine_id` composes `MachineGuid` with the computer name precisely because `sysprep /generalize` moves one and a rename moves the other — but an image deployed with NEITHER gives identical values for both, hence an identical seed and an identical domain. Self-limiting: such a fleet also has duplicate NetBIOS names and would be failing in more visible ways. `EDUBOTICS_MACHINE_ID` is the per-PC escape hatch.
  - **The finalize marker's `user=` field is parsed TWICE, in two languages, and the pair is now fenced (2026-08-03).** `gui_app.py::_prompt_finalize_install::_marker_user` (Python, `utf-8-sig`, U+FFFD ⇒ unparseable, cut at `" distro="`, casefolded compare) and `preflight_system.ps1` check 5 (PowerShell, `Get-Content -Encoding UTF8`, the same U+FFFD rule, the same cut, `-ine`) implement the same decisions independently, and the PS side cannot be imported. Split across two files on purpose, each holding what it is the right place for:
    - `test_gui_install_lifecycle.py::PreflightAccountScopeTest` — the shared marker PATH, the U+FFFD refusal (`[char]0xFFFD` asserted on the **PowerShell** side; the Python half is pinned behaviourally by the sibling `test_an_ansi_written_marker_falls_back_instead_of_accusing`), the UTF-8 encoding ARGUMENT, and the byte-equal `" distro="` cut. Before that pairing landed, the encoding and the cut were asserted in comments and in `CLAUDE.md` and guarded by nothing, and a mutation removing either left all 849 tests green while producing the exact wrong-account accusation check 5 exists to prevent — the U+FFFD refusal cannot catch it, because a cp1252 decode of UTF-8 yields VALID characters.
    - `test_finalize_marker_twin_lockstep.py` — the twin file the other lockstep tests have (`test_arm_scan_twin_lockstep.py`, `test_robot_profile_lockstep.py`, `test_ros_domain_twin_lockstep.py`): the shared `user=` FIELD KEY on both sides plus its PowerShell skip-offset, and that BOTH readers compare case-INsensitively. Phrased over extracted structure — the Python side by AST (both `Compare` operands must be a `casefold`/`lower`/`upper` call, so folding one side alone fails), the PowerShell side by a comparison-operator scan (no `-c` form, and no `String.Equals`/`Compare`, which carry no operator at all) — rather than as substrings, so a differently-spelled violation fails too. Its zero-file floor asserts both files were found, both parses produced a body, and both extracted symbol sets are non-empty; every assertion below it is a set comparison that NOTHING satisfies.
    - **What remains uncompared, and why it is not a gap**: both sides take the name from the FIRST matching `user=` line and stop. That divergence is UNREACHABLE — every marker shape `finalize_install.ps1` writes is a single `Set-Content`, and the only multi-line shape (`FAILED`) carries no `user=` at all — so no marker in the field can hold two candidate lines for the two readers to disagree about. Note the PS side's case check is deliberately broader than needed: it refuses a case-sensitive operator anywhere in the predicate, including on the `$markerUser -ne ""` emptiness test where case cannot matter.
  - **A cp1252 marker written by an OLD finalize degrades silently on an upgraded PC.** The marker is written fresh on every finalize run, so the mojibake case only survives where the LAST finalize predates the `-Encoding UTF8` pairing. That state behaves CORRECTLY — a U+FFFD in the parsed name is treated as unparseable, so both readers fall through to the generic German message rather than accusing the wrong Windows account — but the per-account diagnosis then never fires on exactly the PC that most needs it, and nothing tells anyone why: no log line distinguishes "no marker" from "a marker this build cannot read". Re-running the installer (or any finalize) fixes it by overwriting the marker. Cheap improvement if ever touched: log the U+FFFD case as a distinct German line instead of returning "".
  - **The Blockly autosave's un-namespaced fallback bucket — FIXED (2026-08-03), with one inherent residual.** `components/Workshop/useAutosave` keyed IndexedDB as `` scopeKey ? `${STORAGE_KEY}:${scopeKey}` : STORAGE_KEY `` with `scopeKey = s.auth.session.user.id` — so EVERY session with no user id shared ONE bucket, and the student app *was then* fully usable without a cloud login (only Training and Inferenz gated on it), so two record-only students on a shared PC restored each other's workspace. **The 2026-08-31 student login gate has since narrowed that population to the „Ohne Anmeldung fortfahren" offline escape without removing it — the namespacing stays, all of it.** Both previously-considered fixes really were worse (disabling autosave removes crash recovery from exactly the never-signing-in population; clearing on sign-out cannot help a student who never signs out) — the third option is the one that shipped: **every bucket is namespaced**, the un-scoped case by `autosaveSessionScope()`, a per-browser-session id in sessionStorage. sessionStorage is the carrier because crash recovery must survive a RELOAD while the bucket must not survive the WINDOW, and on the student surface the tab IS the WebView2 window `EduBotics.exe --webview` spawns. The signed-in path is byte-identical. The pre-namespacing BARE bucket is deleted once on mount, by literal name and never via `idb-keyval`'s `keys()` (a pattern sweep could match a real `:<user id>` bucket and destroy work). `edubotics_workshop_autosave_session` is classified in `IGNORED_STORAGE_KEYS` — student-scoped by the operative rule, but `clearStudentScopedStorage` is a localStorage loop that would silently no-op on a sessionStorage key, the same reason `edubotics.jetson.reconnect` sits there.
    **RESIDUAL, inherent to "per session" and not to the implementation**: two students who hand the PC over WITHOUT closing the WebView2 window share the browser session, hence the bucket. Closing the app — the ordinary handover on a shared PC, and the only way the student surface is reached at all — ends it. Rotating the id on `session/signedOut` was considered and does NOT close this: a student who never signs in never signs out, which is exactly the population the fallback bucket serves, so it would only narrow the sub-case where a sign-out happens between the two. That is the same argument that kept the bucket out of the sign-out scrub in the first place, and the cost is a second owner for a key whose whole point is that `useAutosave` owns it alone (plus a new imperative sign-out step needing its own fence).

    **RE-SCOPED 2026-08-31 by the student login gate — narrowed, NOT closed.** On the ordinary path a student now IS signed in, so `WorkshopPage`'s `scopeKey = s.auth.session.user.id` is a string, `useAutosave`'s `scopeKey || autosaveSessionScope()` short-circuits, and the bucket is keyed to the ACCOUNT — the session-scoped fallback is not involved at all. **The residual survives only for a student on the „Ohne Anmeldung fortfahren" offline escape**, who indeed never signs out; rotating on `session/signedOut` still does not close that sub-case, for the same reason, and the cost of trying is unchanged. Worth stating alongside it, because it closes a data-loss path rather than a disclosure one: since the account-keyed bucket is stable across windows and `bootScrub` never touches IndexedDB, **a student whose window is destroyed — by „Arme scannen", by „Umgebung stoppen", by a crash — logs back in and gets their Blockly work back.**
  - **The React suite's flakiness was REAL, was in a safety test, and is fixed (2026-08-03).** ~1.8 % of default-parallel suite runs (4 of 220 on pristine HEAD) failed `JogPanel.test.jsx`'s limp-arm `pagehide` re-torque guard. Root cause proven by instrumentation and it is a TEST bug, not a production one: the test awaited `findByRole('Arm festsetzen')`, whose MutationObserver resolves on a DOM mutation produced at React's COMMIT, while `handGuideOnRef.current` is written by a `useEffect` in the PASSIVE-EFFECT phase scheduled after it — so when the microtask won, `refixArm` read a stale `false` and returned early. A real `pagehide` cannot land in that microtask window. It now gates on `onHandGuideChange(true)`, produced by a second effect declared AFTER the ref-writing one (React runs a commit's effects in declaration order), and 60 consecutive full-suite runs in default parallel mode were clean (extended to 500 on 2026-08-03, still 0).

    **A SECOND flake of the same class lived two tests above it in the same file, and is also fixed (2026-08-03).** `freischalten calls handGuide(true) and disables jog…` gated on `waitFor(handGuide).toHaveBeenCalledWith(true)` and then used the SYNCHRONOUS `getByRole('Arm festsetzen')`. `toggleHandGuide` CALLS `handGuide(enabled)` and only then awaits it before `setHandGuideOn(enabled)`, so the mock is recorded strictly BEFORE the state that flips the button label — a race a verifier reproduced once in 200 runs (~0.5 %; not re-measured here, and it is a race by construction whatever the rate). Fixed with `findByRole`, which is sufficient HERE precisely because nothing at this call site depends on a passive effect, only on the rendered DOM (that difference is why the pagehide test above needs the stronger `onHandGuideChange` gate and must not be 'simplified' back to `findByRole`). **A THIRD instance was assessed and deliberately NOT changed**: the FE-4 keepalive test does `fireEvent.click` then `await act(async () => { await Promise.resolve(); await Promise.resolve(); })` then a synchronous `getByRole`. It is not the same mechanism — the `await` on the mock's already-resolved native promise enqueues its continuation BEFORE `act`'s first `await Promise.resolve()`, and `act` flushes React's work queue on resolution, so the ordering is deterministic rather than raced. It is fragile-by-construction (a microtask-count guess), but the obvious repair is an ASYNC query, and that test runs under `vi.useFakeTimers`, where RTL's `waitFor` detects the sinon clock and advances timers itself — i.e. the fix would put timer advancement inside the one test whose subject is a 15 s interval. Left alone; **500 consecutive full-suite runs in default parallel mode were clean** (0 failures — 91.8 % power against a 0.5 %-per-run rate, >99.9 % against the 1.8 % rate the pagehide flake had). **Related, and the reason a flake in this file matters more than it did:** `vite.config.js`'s `test:` block had no `include`, so vitest's default glob covered the whole package — a stray test file outside `src/` joined the blocking gate unreviewed (measured: 49 files → 51) while being invisible to `sessionScope.test.js`'s `src/`-only coverage walk. `include` is now pinned.

    The earlier claim in this file that the flakiness "did not reproduce" was a measurement gap, not evidence — 20/20 was too small a sample for a 1.8 % rate. What is still unexplained is the OTHER symptom that verifier reported: `signOut.test.js` failing 5/9 with 0–1 ms SYNCHRONOUS assertion failures, which is not derivable from the code and has never reproduced here. The `setupTests.js` storage-mock rewrite remains a latent-hazard fix, not a cause: under vitest 4 + jsdom `globalThis === globalThis.window` (probed), so the old per-target factory call could not produce two Maps.
  - **vitest IS in CI now (`ci.yml::react-tests`, 2026-08-03) — CLOSED.** The whole suite runs on every push/PR, so `sessionScope.test.js` (the STUDENT/MACHINE literals, the storage-key coverage scan, the "exactly one `supabase.auth.signOut()`" glob, the slice-handler structural guard), `signOut.test.js` and the 47 previously-orphaned files are all blocking. `react-lint` stays eslint-only; the two are separate jobs sharing one node/`npm ci` block. What remains bounded is the coverage scan's REACH, not its enforcement: a Storage receiver arriving by a route no pattern can follow (a function return, a property on an object built elsewhere, or a TWO-HOP alias — see the unfenced-invariants list below) is invisible — literal and once-aliased receivers are resolved, and every other Storage-yielding expression is REFUSED as unresolvable rather than passed.
  - **ACCEPTED COST: a spurious 401 destroys the Redux copy of unsaved Blockly work.** `useMeProfile` routes 401/403 through `signOutStudent({reload:false})`, and the `session/signedOut` handler in `workshopSlice` clears `unsavedBlocklyJson` — which is deliberate (WorkshopPage seeds the editor from it when the cloud fetch fails, so leaving it would hand the next student the previous one's program). Three mitigations make the cost bearable rather than fixing it: supabase-js auto-refreshes, so a plain expiry rarely reaches the 401 branch at all; `reload: false` means the live Blockly workspace keeps its blocks in the DOM, so the student can still save; and the IndexedDB autosave is NOT scrubbed and is namespaced per Supabase user id, so the work survives a re-login as the same student. Not fixed on purpose — the alternative is a per-slice "was this involuntary?" flag threaded through the broadcast, which reintroduces exactly the caller-maintained knowledge `session/signedOut` exists to remove.
  - **RESIDUAL: the finalize marker's DIRECTORY is resolved differently by its two readers.** `gui/app/constants.py::diagnostics_dir()` falls back `%ProgramData%\EduBotics\logs` → `%LOCALAPPDATA%\EduBotics` → `~` on a real create-probe failure; `preflight_system.ps1` hard-codes `Join-Path $env:ProgramData "EduBotics\logs"` with no fallback. So on an unwritable ProgramData leaf — precisely the managed-PC population the account-scope check targets — the GUI writes the marker where the preflight never looks, and check 5 degrades to its ambiguous `WARNUNG` (which names both causes and prescribes a remedy fixing either, so it FAILS SAFE). Deliberately not restructured: giving the PS side the same three-step probe means duplicating a resolver in a second language, which is the drift class the marker already suffers from. `test_the_marker_path_is_the_one_the_gui_writes_and_parses` pins the PRIMARY path both sides use; the DIVERGENCE below it — one assignment, no fallback, on the PS side against `diagnostics_dir()`'s real three-step probe — is pinned by `test_the_two_readers_agree_on_the_primary_dir_and_diverge_below_it`, which since 2026-08-03 counts `$DiagDir` assignments at ANY indentation and allowlists the environment variables the script may read at all (its previous column-0 anchor plus a `$env:LOCALAPPDATA` name check both passed an INDENTED `$env:TEMP` fallback — mutation-proven). What still cannot be pinned from the PS side is which directory the GUI actually chose at runtime.

    **A third path divergence, undocumented until now**: `finalize_install.ps1`'s own `-MarkerPath` DEFAULT is `%TEMP%\edubotics_finalize.marker`, not the diagnostics leaf. Every shipping flow is fine — the GUI always passes `-MarkerPath` explicitly (`gui_app.py::_prompt_finalize_install::_finalize_worker`), and the `.iss` never invokes the script at all — but a support engineer or a teacher running the elevated one-button repair BY HAND writes the marker where check 5 never looks, reproducing the same fail-safe ambiguous `WARNUNG`. Cheap fix if ever touched: default the parameter to the ProgramData leaf.
  - **`ci.yml::german-strings-lint`'s grep step barely reaches the installer and GUI trees.** It only inspects lines containing a LITERAL `[FEHLER]`/`[WARNUNG]`/`[STOPP]`. Measured: the whole `robotis_ai_setup/installer` tree has exactly ONE such line and it is a comment (`preflight_system.ps1` builds the tag at runtime inside `Emit`), against ~74 PowerShell lines carrying a quoted umlaut; `robotis_ai_setup/gui` has FOUR, three of them real strings, against ~250. So `PreflightAccountScopeTest::test_the_german_messages_use_literal_umlauts` is the only guard on the preflight wording, and the vast majority of GUI German (which goes through `self._log` with no tag) is unguarded. The AST step is a different surface entirely (cloud-API `detail=`/`error_message=` + pi_agent `message=`).
  - **The Pi's own `ROS_DOMAIN_ID` cache is vacuous in the same way the deleted Windows one was — and must NOT be deleted for symmetry.** With `/etc/machine-id` readable the re-derive returns exactly what the cache holds, so the hit is a no-op; the cache only does real work in the one case where it is genuinely correct to, namely when machine-id is unreadable and the seed degrades to `uuid.getnode()`. `/var/lib/edubotics/` is machine-local and cannot travel with a profile, so the Windows objection does not apply. `tests/test_ros_domain_twin_lockstep.py::test_the_cache_divergence_is_intended_not_drift` fails if the Pi's persist is removed.
  - **The identity gate on `/task/status` silently changed behaviour for students who never sign in, and it emptied a wire field.** `useRosTopicSubscription`'s adopt of `task_info.user_id` is now an identity comparison against `auth.hfUsername`, which is null until `/me` resolves — so a student using the recording path with NO cloud login (reachable, since the 2026-08-31 student login gate, only via the „Ohne Anmeldung fortfahren" offline escape; it was fully supported when this was written, when only Training and Inferenz gated on a session) no longer inherits the `user_id` of a task started elsewhere. They keep their own persisted Benutzer-ID, in a field that is read-only while a task runs anyway, so the practical cost is a resumed task showing the local selection rather than the robot's. The second half is a wire field that quietly stopped carrying data: `taskStatus.userId` is now always `''` unless the robot names the signed-in student. Verified inert — its only other occurrence in `src/` is `taskSlice`'s own reset, i.e. **zero readers**. Recorded because "a field that is always empty" is how a future reader concludes the wire never had it.
  - **`logoutBlockReason`'s liveness gate is a deliberate ONE-WAY trade.** All three block branches require `heartbeatStatus === 'connected'`, so after a rosbridge drop „Abmelden" is PERMITTED while a server-side recording may still be running — the recorder does not care whether the browser can see it. That is the chosen direction: the alternative latched the button shut for the life of the document on the shared PC this whole mechanism exists to hand over. The only remaining net is `StudentApp`'s `beforeunload` prompt, which is NOT liveness-gated and still fires off the stale-true `taskStatus.running`; a student who accepts it signs out over a live recording. Sound, and left alone — clearing `running` on the disconnect edge was rejected for a stronger reason (it is also read by the InfoPanel/InferencePanel editability gates and by RecordPanel).
  - **Three invariants in this change set were assessed and deliberately left UNFENCED**, each with the mutation that defeats it, so a later reader does not have to re-measure:
    - **`_read_machine_id`'s `sys.platform != "win32"` early return** — deleting it is structurally uncatchable off Windows: the very next statement is `import winreg` inside `try/except ImportError`, which returns the SAME `None` on macOS/Linux, so no non-Windows test can tell the two apart. It is defense-in-depth (it keeps the module importable and skips a pointless probe), not a behavioural gate. **One test WOULD catch it** and is worth naming rather than hand-waving: inject a fake `winreg` into `sys.modules` WITHOUT patching `sys.platform`, and assert `_read_machine_id()` still returns `None`. That asserts the gate, not the mock. It is skipped because the state it describes — a non-Windows host on which a module named `winreg` imports successfully — does not occur outside a test, so the guard would fence a scenario only the guard can create.
    - **Check 5's FIRING condition.** `test_the_check_exists_and_tests_both_halves_of_the_split` is an `assertIn` of the literal `$vhdxPresent -and (-not $distroPresent)`, so prefixing the `if` with `$false -and ` leaves it green and silences the whole check. Realistic mutations ARE caught: flipping `-and`→`-or`, dropping either half, or negating one all change the pinned substring. Left as is — a PowerShell reachability test needs a PowerShell interpreter, which no CI runner here has.
    - **A TWO-HOP storage-receiver alias.** `sessionScope.test.js`'s scan resolves a receiver spelled literally or aliased ONCE (`const ls = window.localStorage`) and REFUSES every other Storage-yielding expression — but `const a = localStorage; const b = a; b.setItem('edubotics_x', v)` matches neither pattern: `b`'s initializer contains no `localStorage` token, so `STORAGE_DECL_RE` never sees it and it is not reported as unresolvable either. Same class as the function-return residual the module header already admits, and named there now too. Fixing it means transitive alias resolution, i.e. a small dataflow pass in a test.

    (A fourth candidate was checked and is NOT a gap: `_marker_user`'s `errors="replace"` is what turns a pre-fix cp1252 marker into the U+FFFD the refusal keys on, and removing it makes the read raise `UnicodeDecodeError` — a `ValueError`, which `except OSError` does not catch. Mutation-verified: `test_an_ansi_written_marker_falls_back_instead_of_accusing` already errors on it, because its fixture writes exactly those bytes.)
- **The Windows `.exe` build HARD-GATES the entire Orange Pi release, and that direction is undocumented.** In `release.yml`, `pi-agent-tarball` carries `if: … needs.installer.result == 'success'` and W6 `publish-gui-version` carries the same condition. So a failed `.iss`/installer build skips BOTH Pi-facing jobs — no `edubotics-pi-agent.tar.gz` attached, no Railway flip of `PI_AGENT_SHA256`/`GUI_VERSION` — *after* W1 supabase, W2 cloud-api, W3 teacher-web and W4 docker (including all three `-opi` images) have already landed. The `-opi` images publish and the fleet is never told. The REVERSE direction is deliberate and documented at the W6 grant site (a Pi-tarball hiccup must not withhold the mature `.exe` advertisement, hence `pi-agent-tarball` is an ORDERING-only `needs`); nothing states this direction as a decision. Whether the Pi release should be able to proceed without the `.exe` is an owner call, not a bug fix.
- **The Windows wizard form is TALLER THAN ITS OWN DEFAULT WINDOW, and nothing signals that there is more below.** Measured after the 2026-09-01 visual pass, in a fully realized 700x830 window on macOS/Tk 9.0: **1148 px of content on `omx_full`, 1030 px on the two leader-less profiles** (which is what hiding Schritt A buys them). So the vertical scroll container is load-bearing ALWAYS, not only on small screens — a 1366x768 student laptop must scroll ~460 px to reach „Umgebung starten“ and the Protokoll. **Half-fixed**: the `wraplength` CLIPPING is gone (`gui_app.py::_hint_label` registers every helper paragraph and `::_build_scrollable_container`'s canvas `<Configure>` re-wraps them to the real width, so a 620 px paragraph in a 501 px container no longer becomes unreachable text behind a vertical-only scrollbar), the Protokoll's default height dropped 12→8 lines and the button row's padding 15→10, and `minsize` rose to (640, 520). **The re-wrap is PER LABEL, and it has to be**: the first cut applied one flat `canvas.width - 40` to all of them, which is correct for a paragraph packed straight into the scroll container and 8 px too generous for one nested in a `ttk.LabelFrame(padding=10)` — measured, the Schritt-D HuggingFace paragraph still asked for 589 px in a 581 px container and its own render test caught it. `gui_app.py::_hint_wrap_width` measures each label's LEFT INSET against the canvas (`winfo_rootx` difference) and doubles it for the symmetric right side, which is right at every nesting depth including ones added later; tuning the constant only moves which depth is wrong. The inset is read from the CURRENT layout while handling the configure for the NEW one, which is sound because padding and borders are width-independent. `HINT_WRAP_FALLBACK_INSET_PX` covers the unrealized/implausible/dead-widget cases and reproduces the old flat margin. The remaining overflow is not fixed. The three options assessed, cheapest first: give the Protokoll a collapsible header; hoist the status bar + Start/Stopp row OUT of the scroll container so they are always on screen; collapse Schritt A/B into one frame on both-arms profiles (which now fights `_apply_robot_type_labels`'s hide/show, so it is the expensive one). **Pixel figures are macOS/Tk 9.0 with a fallback font — re-measure on Windows before quoting them.** The MECHANISM is font-independent.
- **Two helper labels still carry a FIXED `wraplength=620`** — the ones `gui_app.py::_on_cameras_changed` builds inside `camera_role_frame` („Schau in die beiden Live-Vorschauen …" and the one-camera cap notice). They are deliberately outside the re-wrap registry: that frame is destroyed and rebuilt on every camera selection change, so registering them means either leaking dead widget references or pruning them on every resize. They are short and sit inside Schritt C, so they clip later than the six that were fixed. `tests/test_gui_layout.py`'s no-hardcoded-`wraplength` assertion is scoped to `_build_ui` for this reason.
- **The wizard's THREE modal dialogs were not brought into the theme.** `gui_app.py::_show_manual_elevation_fallback`, `::_show_update_dialog` and `::_show_phone_url_dialog` keep their own font SIZES (a phone URL is deliberately large) — only the family is single-sourced through `theme_mod.FONT_FAMILY`. `tests/test_gui_theme.py::test_the_main_window_names_styles_instead_of_font_literals` is therefore scoped to `_build_ui`. Converting them is cosmetic-only work with no student-visible defect behind it.
- **DPI is PROBED, never forced, and the Windows baseline is still unmeasured.** `gui/app/theme.py::apply_scaling` sets `tk scaling` ONLY when `process_is_dpi_aware()` says Windows is not already bitmap-stretching the window; on a non-aware process it does nothing, because scaling Tk on top of a stretch enlarges the text twice. `build.spec`'s `EXE(...)` still declares no `manifest=` and CI installs `pyinstaller` unpinned, so **which branch a shipped `.exe` actually takes is decided by whatever bootloader manifest the latest PyInstaller emits on release day** — that is exactly why it is probed rather than assumed. **Open, and an owner call**: screenshot the real `.exe` at 100 %, 125 % and 150 % before deciding whether to add a manifest (and, with it, whether to pin PyInstaller). Both were explicitly out of scope for the 2026-09-01 pass.
- **`device_manager.diagnose_usb_environment` still sets `diag.ok = True` on its terminal FAILURE path.** The student-facing sentence was fixed (it no longer opens „Alle Systeme einsatzbereit" and no longer names `identify_arm.py`), but the flag was left alone deliberately: `grep` finds ZERO readers of `.ok` anywhere in `gui/` or `tests/`, and the field is genuinely ambiguous — every CHAIN check (usbipd, WSL, image) really did pass. Flipping it is a free, invisible change today and a trap the day something starts branching on it. Decide the semantics before adding the first reader.
- **`gui_app.py` has TWO scan buttons now, and they can only be kept in step by hand.** A tkinter widget cannot be re-parented, so hiding Schritt A on a leader-less robot type needs `btn_scan_arm` in the arm frame beside `btn_scan_leader` in the (hidden) leader frame. Exactly one is packed at a time (`::_apply_robot_type_labels`) and `::_scan_arms` disables and re-enables BOTH — a stale enabled twin would let a second worker race the first onto the same `/dev/serial` ports. `tests/test_gui_worker_guards.py` and `test_shutdown_teardown.py` pin the transitions of both, but nothing structurally prevents a THIRD scan entry point from forgetting one.

- **Roboter Studio, 2026-09-09 block-audit round — what was deliberately NOT changed.**
  - **„Pause" does not stop the arm mid-motion; „Stopp" does.** `ctx.wait_if_paused` is called only at BLOCK BOUNDARIES (`interpreter.py`, three sites); `chunked_publish` and `_publish_motion` contain no pause check at all, while `chunked_publish` DOES poll `should_stop` per point. So pressing Pause during „bewege zu" shows „pausiert" while the arm finishes the move. That is the right call — a truncated trajectory is worse than a completed one — and it is also why the rejected „release the lock during a pause" fix would have MOVED the arm. Recorded, not fixed: a teacher will meet it.
  - **[RESOLVED 2026-09-09 — the field is DELETED.] `ArmProfile.grasp_held_max_rad` had ZERO production readers.** `motion._grasp_held_max` resolved it from `ctx.grasp_held_max_rad`, and `WorkflowContext` has no such field — `workflow_manager.start()` never stamped one — so every production run already fell through to the DERIVED branch `gripper_closed_rad + grasp_held_margin_rad`, which yields the identical number on all four profiles (measured before and after: −0.3500 / −0.3500 / 0.1200 / 0.1000). Of the two honest answers the owner took the second: the field, its two profile rows and the dead resolution rung are gone, the derivation stays, and `motion._grasp_held_max`'s docstring now says that re-adding the lookup requires declaring the matching `WorkflowContext` field IN THE SAME CHANGE. The field appeared in NO other copy of the registry (neither `gui/app/constants.py` nor `pi_agent/constants.py`) and in NO test, so the three-way lockstep was untouched. **The counterpart landed the same day and is wired**: the newly declared `WorkflowContext.gripper_knob_warned` is proved by a test that drives the real dataclass, precisely because a dead `getattr` is the failure mode this file had already paid for once.
  - **[RESOLVED 2026-09-09 — WARN, never clamp.] The two operator gripper knobs are Rule §2 surface and were guarded by `math.isfinite` alone.** `EDUBOTICS_PICKUP_CLOSE_RAD=5.0` on an edu6 (band 0.00…1.75) reached the published trajectory's gripper column and `=-9.0` likewise; `_held_threshold_rad`'s first branch returned the RAW module constant (−0.35) when the env was non-empty, and on a Feetech rig that constant sits below the whole band, so setting the documented rollback made „Greifer hält etwas?" constant-TRUE for every close. `motion._warn_gripper_knob_out_of_band` now emits ONE German `[WARNUNG]` per run naming the knob, its value, the arm's band and (for the HELD knob) the consequence — **and then uses the value**. **Clamping is the wrong answer and the helper's comment says so in place**: `EDUBOTICS_GRASP_HELD_MAX_RAD` EXISTS to restore an out-of-band constant as the documented one-variable rollback, so clamping would silently disable the very rollback it is, and refusing would too. This is deliberately NOT `close_on_object`'s treatment of a CATALOG angle (which warns AND falls back) — a catalog number has a documented fallback and no rollback meaning. The documentation half is fixed too: the comment that called `EDUBOTICS_PICKUP_CLOSE_RAD` „the generic close for the whole rig" now names its two real callers („aufnehmen" and `close_on_object`'s fallback) and states that „schließe Greifer" commands `_gripper_closed(ctx)` unconditionally, so a rig opted into the bounded squeeze keeps a full-stall „schließe Greifer". **Still open, smaller, same family:** an UNPARSEABLE `EDUBOTICS_PICKUP_CLOSE_RAD` (`abc` → NaN) is still silently ignored and falls through to the profile default — no warning names the knob the operator set. Out of scope for this round by instruction; the band warning deliberately does not fire on a non-finite value, because a sentence about a band would name a comparison that was never made.
  - **`EDUBOTICS_GRASP_Z_FROM_PLANE`'s flat-table tail was re-derived and is REAL, and the knob stays ON.** No change proposed here; it is listed so the next reader knows the tail was measured rather than assumed, and that turning it off is a documented one-variable rollback.
  - **`_warn_unreadable_zones` is narrower than a reader expects, in two ways, and neither is fixable there.** (a) The only shape it can see is a LIST of malformed boxes: a NON-list never reaches `ctx.zones`, because `WorkflowManager._parse_zones` returns `None` for it and every consumer's `if zones:` is then False — so such a payload is neither warned about nor protected. (b) The warning fires from `safe_move` and `replay_trajectory`, i.e. on TRANSIT legs, so a program that draws zones and performs no transit („Greife" + „hebe an" only) is never warned. Per-entry malformed COUNTS are nobody's job today; the read-only companion that was supposed to serve them had zero production callers and was deleted.
  - **`build_object_catalog_response()` is still called with no `profile_id`**, so the `GetObjectCatalog` wire response carries the OMX catalog on an edu6/edu1 rig (React dropdown labels + the sim stage's render dimensions). Pre-existing, documented in `CLAUDE.md`, and deliberately deferred to „Eigene Objekte", which rewrites the same builder — fixing it here would be the one change with no verification behind it.
  - **[NEW 2026-09-09] A plane-tracked pin's workspace-floor cross-check is now definitionally satisfied, and that is the accepted cost of the staleness fix.** A camera pin keeps (x, y) and re-asks `motion.table_z_at` at run time (`motion.resolve_destination_z`), so `pin_z == _floor_z_at(pin_x, pin_y)` EXACTLY and the refusal `target_z < floor_z − WORKSPACE_FLOOR_MARGIN_M` can never fire for that ONE target class. Audit §9.2(b) already recorded this for the service-persisted pin the previous round shipped; this round extends it to the block-carried `Z` field. **The floor still judges every other class** — „Position merken" captures, detections, drop targets, raw coordinates — and `test_sim_node_wiring` pins BOTH halves (the refusal through a measured destination, and the pin's new immunity) so the loss cannot hide behind a green suite. On a good plane it is strictly better (it removes a false refusal of a legitimate pin); on a bad plane it converts „refuses" into „descends to the fitted plane", which is the owner-authorised RS-07 trade.
  - **[NARROWED 2026-09-14 — a `destination_ref` that no pin/current statement sets is now checked against the run's stored destinations with `resolve_destination_z` under the run's calibration (`WorkflowManager._precheck_destination_points`); the rest of this entry stands for PIN statements.]** **[NEW 2026-09-09] `_ik_precheck` still reads the BAKED `Z` field.** `Interpreter.collect_concrete_destinations` builds its targets from the Blockly fields, so the non-fatal German reachability warning is computed against a height the run itself no longer uses. The delta is millimetres and the output is advisory (`Diese Position ist außerhalb des Arbeitsbereichs.`), so it was left alone rather than widened into `start()`; a pre-check whose verdict flips on 8 mm would be a different problem. Recorded so nobody reads the asymmetry as an oversight.
  - **[CORRECTED 2026-09-11 — the premise was wrong: until that date the backpack module never even LOADED in a browser (its `import()` ran after `Blockly.inject` and threw „CSS already injected"), so there was no stash to lose. Since the plugins load before inject and initialise before the initial load, a stash DOES come back through an AUTOSAVE restore; the cloud SAVE still drops it, so a workflow switch or version restore empties it. The rest of this entry stands.]** The student's Blockly BACKPACK no longer survives a reload. `WorkshopPage.handleSave` now ships an allowlisted document (`blocks`, `variables`, `workspaceComments`) into `workflows.blockly_json`, dropping `backpack` and `suggested-blocks` — see `utils/blocklyPayload.js` for the two measured reasons (a document that eventually cannot be saved, and one student's private clipboard riding into a template a teacher publishes or a sibling clones). The backpack is persisted ONLY through that serializer (the plugin touches no localStorage), so a stash now lives for the session. Loading a workflow already emptied the backpack to whatever that workflow carried; now it empties it, full stop. **The durable fix is a student-scoped LOCAL home for the stash** — it would be registered in `utils/sessionScope.js` beside the three `blocklyStash*` multiselect keys, which are already there for exactly this reason. Feature-sized, not this change. AUTOSAVE deliberately keeps the FULL serializer output: it is local, per-student and never shared, and CLAUDE.md protects it by name.
  - **[NEW 2026-09-09] „zähle von 1 bis 100: melde i" prints all 100 lines; the burst cap is 500 and IS reachable.** `OUTPUT_BURST_LOG = 500` (2.5× the React Protokoll's own 200-line cap) replaced the 50 that made a 100-iteration first-lesson program look like it had stopped. A long enough straight-line loop still truncates, with a German `[WARNUNG]` saying only part is shown. **One-variable reversible**: `EDUBOTICS_OUTPUT_MAX_PER_S=0` disables the bucket entirely, forwarded on both composes. Any loop with real pacing refills it. Recorded as a named trade, not a regression — the bucket was chosen against a measured flood (17 overlapping utterances, 265 stacked toasts).
  - **[NEW 2026-09-09] `capture_pose_callback`'s DEGRADED name check still answers the bare „Ungültiger Ziel-Name.".** Its normal path now uses the shared `destination_name_error_de` sentence; the `except` fallback that fires only if importing the shared validator fails keeps a local charset regex AND the old bare text on purpose, because reproducing the alphabet sentence there would be the second copy the change removes. Unreachable in practice (the import is a sibling module), and the fail-CLOSED behaviour is the point.
  - **The L0b routed/refused counts in `home_planner.py`'s comment are not reproducible** (an independent sweep following the stated protocol got materially different absolute numbers). The DIRECTION and SHAPE reproduce; the zone-sampling distribution was never recorded. The comment now says so; re-derive with a stated distribution before quoting the table.
- **Roboter Studio, 2026-09-11 block-canvas layout fix — what was found and deliberately NOT changed.** (The fix itself: `CLAUDE.md` „Editor UI invariants", the Blockly-canvas bullet.)
  - **Switching to „Galerie" is unguarded (found by code reading, not run).** `WorkshopPage` renders ONLY the gallery strip in that view, so the dock (JogPanel/RecordPanel) and `RunControls` unmount: mid-run the „Stopp" button disappears while the program keeps running; mid-recording or mid-hand-guide the panels' unmount teardown fires (a recording is cancelled, a hand-guide re-torqued). The simulator toggle already refuses exactly these states; the view switch does not. Owner deferred it on 2026-09-11.
  - **`UrdfTwin` has no error boundary.** With no WebGL (a blocklisted GPU, some remote-desktop/VM sessions — reproduced in headless Chromium) `THREE.WebGLRenderer` throws and React unmounts the whole Roboter-Studio page to white; the simulator stage and the „3D-Ansicht" tab are the triggers. Owner deferred it on 2026-09-11.
  - **Two plugin packages are installed but no longer imported**: `@blockly/workspace-minimap` and `@mit-app-inventor/blockly-plugin-workspace-multiselect`. Kept to avoid lockfile churn; `bootScrub.crossTab.test.js` still reads the multiselect dist on purpose. Remove both together with that test's dependency half if they are ever dropped for good.
  - **`BlocklyWorkspace`'s change handler does not skip UI events**, so every zoom step and pan frame serialises the whole workspace into Redux (and `useAutosave`'s listener is equally unfiltered). No long task was measured on a fast Mac; slower classroom PCs were not measured. Pre-existing, left alone.
  - **Below `md`, `SimStage` is a scroll container nested inside `SimScene layout="split"`'s own `overflow-y-auto`**, so a narrow window can show two scrollbars in the simulator column. Cosmetic; the 3D pane keeps its `min-h-[220px]` and nothing is cut off.
  - **A throwing `workspaces.load` leaves „Vorschläge" deaf for the session.** Blockly 12's `load` has no `try/finally`, so a deserializer throw skips `FINISHED_LOADING` (and leaves `setResizesEnabled(false)`); with `waitForFinishedLoading = !!initialJson` the suggestor then records nothing until the next mount. Pre-existing shape, newly reachable through that argument; a corrupt saved workflow is the only way in.
  - **A one-microtask `workspace === null` window on an `initialJson`/`readOnly` change.** Injection now waits for the plugin promise, so a workflow switch briefly reports no workspace: two extra parent renders and a „Code-Vorschau" placeholder flash. Was zero-width when inject was synchronous.
  - **A half-screen window (< 768 px wide) is honest but cramped**: the editor gets half of what is left after header, toolbar, Code strip and run bar (~185 px at 683×730), so the zoom cluster is hidden there and only the trash can shows. That is the accepted outcome of „shrink, never clip".
- **Roboter Studio Sammlung (in progress, 2026-09-14; consolidated when the feature lands).**
  - **Robot-local destinations stay reachable for payloads WITHOUT the `destinations` sibling (legacy clients); `capture_pose_callback`/`mark_destination_callback` still persist robot-locally.** A `/workflow/start` payload that carries the sibling is authoritative (`WorkflowManager.start`, `CLAUDE.md` „Destination precedence"), so the shared-rig cross-student resolution only survives on the legacy path. Removing the robot-local writers is not part of this round.
  - **Sammlung flyout: cards vs Plan B — CARDS ship (spike 2026-09-15, real Blockly 12.5.1 in jsdom, `sammlung/__tests__/AssetCardInflater.test.jsx`).** (a) PASS — the „Aufnahmen" callback opens in the real flyout and `flyout.getContents()` holds an `edubotics_asset_card` item; (b) PASS — `getFocusManager().focusNode(card)` makes the card's `<g>` the `document.activeElement` and `flyoutWorkspace.lookUpFocusableNode(id)` returns the card; (c) PASS — through `flyout.getWorkspace().getNavigator()` the card's next sibling is its prefilled block and the block's previous sibling is the card (a RAW policy returns the `FlyoutSeparator` Blockly puts after every item — only the Navigator skips it); (d) PASS — ▶ pointerdown+pointerup dispatches exactly one `preview` and never calls `getGesture`; a body click (pointerdown+pointerup — a lone pointerup is ignored by `conditionalBind`, which has no captured pointer to match) dispatches `manage`; (e) PASS — re-opening the category and `refreshToolboxSelection()` dispose each card once and unbind all six bindings, `flyout.hide()`/`clearSelection()` dispose nothing, and building a card adds no document/window listener and no provider subscription; (f) PASS — after a ▶ tap with `pointerId 31` a workspace pointerdown with `pointerId 32` still reaches `getGesture`, while the naive default-capture binding (asserted in the same test) leaves the next pointer ignored; flipping ▶'s pointerdown back to default capture fails (f). Plan B stays behind the test seam `__setFlyoutModeForTests('plan-b')` only; its classes are NOT styled in `src/index.css` because nothing ships it. jsdom cannot measure text, so chip widths are an estimate (`5.6 px × chars + 12`) and chips WRAP to further 20-unit rows (`AssetCardInflater.js::layoutChips`) rather than being dropped — the last chips („anderer Roboter", „im Simulator abgelehnt") are the serious ones; rig gate S-R9 covers the real rendering.
  - **Rig gate S-R9 — flyout cards with keyboard, screen reader and touch** (Chromium in the WebView2, a touch screen or pen): open each Sammlung group; focus a card; NVDA or Windows Narrator; on a touch screen tap ▶ then immediately drag a block out of the same flyout and a block on the canvas. Expected: card focus visible and announced („<Name> — <Meta>"); clicking/tapping ▶/⋯ never drags the flyout; after a ▶ tap every later touch still works. OPEN.
  - **Rig gate S-R10 — page layers above the editor** (Chromium in the WebView2 at 700 px and 1280 px window height): open the Sammlung drawer, Vormachen, the HomeGlide dialog and (on an OMX full kit) provoke the CollisionModal while the Roboter-Studio editor is visible; click on the drawer/dialog/modal text right where the Blockly category column is. Expected: the drawer sits BESIDE the category column; the overlay, the HomeGlide dialog and the CollisionModal cover the category column and take the click (the editor host's `isolation: isolate`). OPEN.
  - **Vormachen (`teach/useTeachSession.js`, hand mode) is wired through `teach/TeachOverlay.jsx` + `teach/TeachHost.jsx`; the old „Aufnehmen" dock tab (`RecordPanel`) and the Kamera-tab „Position merken" are retired.** Two server orderings it works around rather than fixes (both are the manual-mode arbiter, ask-first): `workshop_hand_guide_callback(true)` never re-checks `_manual_exit_gen` after waiting for `_manual_lock`, so a `hand_guide(false)` landing inside a `true`'s claim-to-lock window is undone (arm limp, `on_manual False`, idle watchdog inert); and it stamps activity BEFORE its leader check, so a refused keepalive still refreshes the watchdog. Client mitigation: ONE FIFO service queue per session (a call is sent only after the previous answer), keepalives queued never skipped, teardown (unmount, `pagehide`, offline close) chained behind every queued or in-flight `record start`/`hand_guide(true)` (a keepalive included), a refused keepalive stops keepalives and re-locks. A second tab can still interleave (in the same tab JogPanel's „Arm freischalten" and nudges and the LeaderToggle are locked while Vormachen is open; JogPanel's „Arm festsetzen" deliberately is not). Also: a `pagehide` that kills the page while a `record start`/`hand_guide(true)` is in flight cannot send the chained cancel — the server's idle watchdog (30 s, open hand-guide session) or `RECORD_MAX_S` (120 s, recording) is then the only re-lock.
  - **Rig gate S-R7 — timers under the student's hand** (OMX, edu6, edu1): in `frei` hold the arm still for 60 s; in `fest` press F and watch the countdown; stop a take and wait 30 s. Expected: no re-lock while `frei` (the 15 s keepalive works); the 3-2-1 countdown precedes every torque-off (UI only, not a safety guard); the idle watchdog never re-locks during a take; on edu6/edu1 the student is warned the arm falls when released (hand-guide is torque-off limp by design). OPEN.
  - **Rig gate S-R8 — D5 auto-save end-to-end** (OMX): an unsaved workflow → Vormachen → keep two takes back-to-back → reload the page. Expected: exactly one „Neuer Workflow" exists with both recordings; one toast; captures made during the save are still in the document. OPEN.
  - **✎ on a just-kept recording can answer „Bewegung nicht gefunden."** until the recording list has been re-fetched after its upload (`renameRecording` resolves the row from `studioAssets.trajectories`); the overlay re-fetches on every successful upload, so a second try succeeds. A capture refused by the server keeps its automatic name reserved for the session, so the next capture may skip a number („Position 3" after „Position 1"). Cosmetic, accepted.
  - **Rig gate S-R11 — F at a keepalive tick** (OMX, edu6): in `frei`, watch the 15 s keepalive (server log `hand_guide`) and press F in the same second 10 times; also start a P capture right at a tick. Expected: the arm always ends LOCKED and „Arm ist fest"; the server log never shows `hand_guide(true)` after the `hand_guide(false)` it answered; after a capture at the tick the arm is not re-locked by the idle watchdog within the next 30 s. OPEN.
  - **Rig gate S-R12 — page closed during a start** (OMX): press Space in `fest` and close the WebView2 window during the 3-2-1 / right after it. Expected: the server either never started the recording or re-locks it — an open hand-guide session within 30 s (idle watchdog), a running recording at the 120 s cap. Record the observed time here. OPEN.
- **Deferred operator runs**: `tools/docker-hub-cleanup.sh --execute` (junk repos + `*-dirty` tags; also dispatchable via `docker-publish.yml::cleanup_dirty_tags`), `tools/modal-cleanup.sh --execute` (both default to dry-run).
- **Browser-migration program is PARKED** — full plan + ground-truth study archived in `docs/CLAUDE-CHANGELOG.md`. Its P0 gate (browser WebSerial reads from the OpenRB-150) was benched at 503–713 Hz (100 Hz solid), so the program is viable but not active; `tools/browser-spike/` no longer exists in the tree.
