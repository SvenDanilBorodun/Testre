{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Intentionally empty. The web dashboard is deployed on Railway via Dockerfile.web + nginx.web.conf.template + railway.json. This file is a kill-switch: if a Vercel project is still linked to this repo, the lack of buildCommand/outputDirectory/framework prevents Vercel from shipping a shadow deployment that would bypass the nginx security headers. Do not restore build fields."
}
