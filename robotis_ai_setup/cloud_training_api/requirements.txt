fastapi==0.115.12
uvicorn[standard]==0.34.2
supabase==2.15.1
# Bounded: an unbounded `modal>=0.64` floor once floated to a breaking
# major (1.4.3) on deploy and broke runtime Modal calls. Keep the upper
# bound; 1.4.x is verified working — review before bumping to 2.x.
modal>=1.4,<2
httpx==0.28.1
python-dotenv==1.1.0
pydantic==2.11.3
huggingface_hub>=0.25.0
